<!-- Campaign record. Moved out of the Claude memory store 2026-08-05; the
     memory file was `project_shrewsbury_reinvestigation`. -->

> **Historical record — read top-down, newest first.** Blocks are appended in
> reverse chronological order and later blocks SUPERSEDE earlier ones, including
> where an earlier block calls itself current. Several conclusions in here were
> retracted; the retraction is always above the claim it retracts. For what is
> believed true *now*, see the memory store and `CLAUDE.md`, not this file.
>
> **Standing summary at the time of the move:** 2026-07-08 advisor-driven re-open of the Shrewsbury under-fill — HWM/gauge apples-to-apples audit + N/NW boundary-forcing audit + \"wall\" experiment framing


## ✅ 2026-07-16 — **THE LAST TWO LEVERS ARE CLOSED (E + M). BOTH NULL. `faber-waves-premier` STANDS.**

Jobs 58185237/38/39 ran clean overnight (1h42m–2h05m, 64 threads, full 73-frame window).
Scored via `evaluate` → `experiments/metrics_workstream_MN.csv`. Report sections written +
figures `reports/figures/{motf,hwm}_panels_EM.png` (script: `scripts/figures_workstream_EM.py`).

- **E — infragravity: NULL.** `wave-ig` = premier with ONE line changed
  (`snapwave_igwaves 0→1`, verified by diff). Every metric within ≤0.01 m (HWM bias 0.318 →
  0.317, CSI 0.706 → 0.704). IG was forced HARD (logged `hm0ig` 3.02 m) and still did nothing
  ⇒ the null is strong, not a non-test. **The wave-overtopping-fills-the-back-bays hypothesis
  is dead.** E is closed.
- **M — boundary depth: DON'T ADOPT.** −15 m is a wash. −20 m *nails* the Shrewsbury crest
  (err **+0.003** vs premier −0.099) and lifts CSI 0.706 → 0.724 — **but by raising water
  everywhere** (HWM bias 0.318 → **0.476**). More wetting, not better wetting; MOTF's POD
  rewards exactly that. **Keep `mask_zmin = -10`.**
- **Locality holds** across the sealed arms: south_coast 0.147 flat, Sandy Hook gauge peak
  identical ±0.002 m in all four.
- **⭐ NEW OPEN THREAD (the interesting one):** premier peaks at **hm0 = 7.44 m inside Sandy
  Hook Bay** — looks high for that fetch — and the IG flag **halves it to 3.98 m** while
  touching no water level. Nobody has yet asked whether the bay's *wave* field is right.
- **Correction to the record:** the "CSI 0.51 → 0.64" line credited to the premier was the
  **nowaves** arm. Verified: `faber-nowaves` = 0.643, **`faber-waves-premier` = 0.706**.
- ⚠️ Scoring/figure traps found the hard way — read [[reference_floodmap_cache_traps]] before
  touching the floodmap cache or trusting a clipped HWM number.

---

## ✅✅ 2026-07-14 (WORKSTREAM K) — **THE LEAK FIX WORKED. THE UNDER-FILL IS SOLVED.**

**The 2×2 (`wall`|`extend` × `waves`|`nowaves`, staged by `scripts/setup_leak_fix.py`, mask edit only — no subgrid rebuild) RAN CLEAN and CONFIRMS the leak diagnosis below. Analysis: `scripts/analyze_leak_fix.py` → `reports/leak_fix.csv`.**

**1. THE LEAK IS GONE (three independent checks):**
- Cut velocity **−1.08 m/s → −0.010 m/s**; **flowed OUT 100% of timesteps → 53% on `extend`** (i.e. it REVERSES — a normal tidal cross-section again). `wall` still 96% out (expected: it's a wall).
- Calm-night Navesink drawdown **−1.48 m → +0.09 m** (`extend`) / +0.02 m (`wall`). The unphysical pre-storm draining is gone.
- Mass balance **92.6% missing → −5.7%** (`extend`) / −6.1% (`wall`). **IT CLOSES.**

**2. THE UNDER-FILL IS FIXED (no-waves vs no-waves, apples to apples):**
- **Shrewsbury HWM bias −0.606 → +0.114 (`extend`) / +0.128 (`wall`)** — the ~0.5–0.7 m under-fill chased since June is GONE (now slightly OVER, within noise).
- Shrewsbury gauge peak **1.663 → 2.478 / 2.505** (obs crest 2.935; remaining −0.46 is the missing wave setup — the waves arms decide this).
- Pooled HWM RMSE **0.812 → 0.494**.

**3. IT PASSED ITS OWN FALSIFIABILITY TEST.** The fix was required to move ONLY the basin that leaked. **South coast bias −0.0553 → −0.0553 (identical); Sandy Hook Bay −0.019 → −0.010 (untouched).** The correction is exactly as spatially local as the diagnosis demanded. (Atlantic oceanfront n=3 moves −0.27→+0.37 — these HWMs sit ON the Sea Bright barrier, whose back side is the estuary now holding water; physical, not a violation.)

**4. `wall` ≈ `extend` (within 0.03 m on every metric).** The bracket collapsed: recovering the 2.5 km² of upstream prism barely matters — **what mattered was SEALING the hole, not where the wall goes.** Prefer `extend` anyway (it puts the wall at the true head of tide and its cut reverses properly).

**5. WORKSTREAM J's PARTITION WAS CONTAMINATED.** Throat inflow **3.72e8 → 4.9e7 m³** — the drain was sucking **7.6×** the honest volume through the Highlands throat. Barrier 9.1e6 → 8.0e5. Ratio throat:barrier **41× → ~61×**, so **the conclusion survives (throat dominates, barrier negligible) but every absolute flux number in the J block below is wrong.**

**⚠️ CONSEQUENCE: every pre-K experiment in the campaign ran on a leaking domain (all hard-link the same `sfincs.nc`), so the whole comparison matrix — Faber-vs-Galibier, niter, clamp, wind, narrows, A1/A2/A3 — is built on a broken baseline and its NULLS ARE EXPLAINED, NOT INFORMATIVE. Do not cite them as physics. The premier must be re-established on the SEALED domain.**

**6. ON THE PREMIER'S OWN PHYSICS (waves ON) — the headline table:** gauge 2.223 → **2.691** (`extend`) / 2.720 (`wall`), obs 2.935 ⇒ error −0.71 → **−0.24** (inside the ±0.3 m forcing-noise band). Shrewsbury HWM bias −0.422 → **+0.213**. Pooled RMSE 0.696 → **0.574**. South coast **0.0595 → 0.0595 (unchanged to 4 dp)**, Sandy Hook Bay +0.038 → +0.030 ⇒ falsifiability test PASSED with waves too.

**7. A3 (tidal range) SEALED:** Shrewsbury damping **0.54 m → 0.24 m** (mod range 0.83 → 1.13, obs 1.369). The "version-independent over-damped tide" was largely the leak. **⚠️ `tidal_range_metric` had a KEY-NAME BUG in `analyze_leak_fix.py` (asked for `tidal_range_mod_m`, actual keys are `tide_mod_range_<gauge>_m`) ⇒ silently returned NaN. FIXED.**

## 🚨 NEW DEFECT FOUND 07-14 (spun out of K): **SHARK RIVER INLET IS DAMMED — A SECOND BRIDGE-AS-DAM.**
**The Shark River estuary NEVER FLOODS in ANY run of the entire campaign, premier included: peak zs = EXACTLY +0.00 m (its initial condition) while the ocean 1.8 km east reaches +2.9 m.** It does not rise 1 cm during Sandy. Cause: marching bed elevation across the inlet, the seaward channel is real (−4 to −10 m) and the landward channel is real (−2.7 to −4.3 m), but **the lowest point ANYWHERE on the cross-section between them is +0.57 m — ABOVE MSL** (x≈583,900; sill also +0.14/−0.10 at x 584,000). Shark River Inlet carries the NJ Transit Coast Line bridge + Route 71 + Ocean Ave, baked into the lidar as an earthen dam — **identical signature to the Rumson–Sea Bright causeway ([[project_bridge_dam]]), fix it the same way (carve the surveyed channel).** The only mask-connected path from the Shark gauge to the sea climbs OUT of the channel and runs 1.7 km **overland across Belmar's streets** at +3..+5 m. (Shark IS mask-connected to the ocean — a naive flood-fill says "connected" and is MISLEADING; the blockage is elevation, not topology.)

## 🚨 WHY SHARK HID (corrected 07-14 — my first explanation was WRONG, see [[reference_hwm_metric_blindspot]])
**An HWM records that water ARRIVED, not which way it came.** The model floods Belmar/Avon anyway — **the ocean overtops the beach and runs OVERLAND through the streets** — so the 2 Shark HWMs read **WET** (0.30 m / 2.39 m deep) while the river channel beside them is at +0.00 m. **The model got the right answer in the right place by an entirely wrong pathway and every mark-by-mark metric was blind to it.** Shark had NO working diagnostic: (1) its only 2 HWMs are **quality 3**, below the `q<=2` headline cut ⇒ never scored; (2) even scored they read flooded; (3) its tidal range was `max−min` of the **spin-up drawdown** (no oscillation at all) ⇒ 1.27 m, which looked plausible vs obs 1.82. **FIXED: `_tidal_signal()` now de-trends and uses FRACTION-OF-TIME-RISING (a tide ebbs AND floods; a drain only ebbs) ⇒ Shark now returns `is_tidal=False` in every run.** Traps: the drawdown is EXPONENTIAL (linear de-trend leaves a ~1 m bowed residual) and turning-point counts are defeated by numerical wiggle. Added a `shark_river` HWM basin. **SEPARATE, REAL, BUT NOT THE CULPRIT: `hwm_metrics` did drop dry marks (`head = wet & (qual<=2)`) ⇒ structurally rewards under-flooding (mirror of MOTF-POD). Now scored at ground elevation. But `n_dry = 0` for q<=2 marks in all 6 runs — I blamed this flaw first and it was wrong.** K comparison unaffected: scored counts identical across all 6 runs.

NEXT: (a) carve Shark River Inlet; (b) fix the HWM dry-mark metric; (c) **re-establish the premier on the SEALED domain** (all engine/knob comparisons are void); (d) the **Atlantic-oceanfront bias +0.73 → +1.03 m with waves** is now the LARGEST remaining error and the leak fix left it standing — investigate. Report `reports/shrewsbury_investigation.md` REWRITTEN 07-14 with J + K + Shark + "where this leaves the investigation".

---

## 🚨🚨 2026-07-14 (LATER) — **THE UNDER-FILL IS A MASS LEAK. THE MODEL DRAINS THE ESTUARY. [DIAGNOSIS — CONFIRMED BY WORKSTREAM K ABOVE; THE FIX WORKS. Superseded only in that the leak is now PLUGGED and J's absolute fluxes were contaminated.]**

**Found by job 58159466 (`faber_flux_25m`, the Workstream-J flux partition). The run was fine; the OUTPUT was unphysical, and the unphysicality IS the finding.**

**THE BUG: the model chops the Navesink River in half, mid-channel, and puts a free-outflow boundary on the cut face.**
The active mask ends at **x≈580,670** (y 4,468,700–4,470,400). hydromt assigned those edge cells **mask=3 (outflow / Neumann)**. Immediately west of them the cells are **mask=0 but the bathymetry is WATER, zb down to −5.6 m** — the real Navesink continues 2.8 km further west, to the true head of tide (Swimming River Dam, x≈578,413; Red Bank is 1.2 km west of the cut). So the domain edge is a **5-metre-deep open cross-section of a tidal river with a free-outflow BC on it.** That is a drain, not a boundary.

**MEASURED (all from job 58159466):**
- Cut velocity: **mean u = −0.82 m/s, peak −2.02 m/s, WESTWARD (out of domain) in 100% of timesteps**, from hour 0. Never reverses. A real tidal cross-section reverses every ~6 h; the real Navesink's freshwater discharge is ~2–5 m³/s, and the model runs ~1000× that, one-way, forever.
- Estuary mass balance: throat delivers **−3.72e8 m³ IN**; barrier delivers **+9.1e6 m³ IN**; estuary storage rises only **+2.8e7 m³**. ⇒ **3.53e8 m³ (92.5% of all inflow) VANISHES.** It exits the Navesink cut. The estuary is not a bathtub, it is a **PIPE**: Sandy Hook Bay → Highlands throat → estuary → out the west cut → gone.
- **Two more leaking cuts** exist: **Shark River west** (29 wet cells, mean u −0.39 m/s, 100% out) and the **NW/Raritan corner** (14 wet cells, mean u −1.75 m/s, 100% out).

**WHY THE BIAS IS BASIN-SELECTIVE (independent consistency check — the leak's fingerprint IS the HWM table).** Sandy Hook Bay (+0.038) and the open coast (+0.06) are unbiased because their tidal prisms are ~1e9 m³ — a leak barely dents them. The estuary's **entire tidal swing is only 3.8e7 m³**, so a 3.5e8 m³ leak is **10× its whole tidal swing.** The only basin that leaks *relative to its size* is the only basin that is low (**shrewsbury_navesink −0.42**). This is exactly the "the error is spatial ⇒ the missing physics is local to the estuary" pattern — but the local thing is **a hole, not missing physics.**

**WHAT THIS OVERTURNS (be ruthless — these are in the log below as settled):**
1. **"Water enters OVER the Sea Bright barrier, not through the narrows" → REFUTED BY MEASUREMENT.** Throat 3.72e8 m³ vs barrier 9.1e6 m³ ⇒ **the throat is 41× the barrier.** (Sign verified independently: throat cells flow south at mean v = −1.04 m/s, into the estuary, in 99% of timesteps.) The over-the-barrier story was inferred from crest-wetting + storage-scaling — the two signals `setup_flux_partition.py` was written to say prove nothing. It measured, and it went the other way. **Caveat: the leak drives an artificial suction THROUGH the throat, so the true partition in a sealed model may differ — but the barrier's ABSOLUTE contribution (9.1e6 m³) is negligible either way.**
2. **2026-06-25 A/B ("the 0.21 CSI gap = narrows/conveyance, NOT the boundary") → FALSIFIED.** It IS the boundary — via a **third failure mode neither arm considered.** Arm 1 (waterlevel) *pinned* the Navesink to the ocean surge ⇒ force-flooded it (flattering CSI 0.77). Arm 2 (outflow) *drains* it ⇒ under-fills (CSI 0.56). **That A/B compared two broken options and concluded the boundary was innocent.**
3. **07-05 "residual = DISTRIBUTED conveyance over-restriction near the STRUCTURAL CEILING, not a fixable knob" → PREMATURE.** It is a fixable knob: a mass sink.
4. **EXPLAINS THE ENTIRE WALL OF NULLS.** Narrows widening, eHydro dredge, niter, dtwave, wind, friction, mesh refinement (L4 12.5 m) all moved the estuary ~nothing **because the basin has a hole in it. You cannot fill a bucket by widening the tap.**
5. **EXPLAINS A3** (tidal range over-damped 0.91 vs obs 1.54, "version-independent"): a continuously-draining basin cannot build tidal amplitude. And **"version-independent" is the tell** — the leak lives in the **mask/mesh**, and *every* experiment in this campaign was staged by **hard-linking the SAME `sfincs.nc`**. Faber, Galibier, every niter/clamp/wind/narrows arm — **all of them inherit the leak.** The comparison matrix is internally consistent and built on a leaking domain.

**THE FIX IS CHEAP — THE SUBGRID ALREADY COVERS THE DEAD CELLS.** `sfincs_subgrid.nc` has `np = 547,267` = **ALL quadtree faces, including the 155,232 inactive ones.** So the deactivated Navesink water (137 wet cells, 2.47 km², subgrid tables already built) can be **switched on by editing the mask alone — NO subgrid rebuild.**
- **Discriminator (hours, no rebuild): set the three wet cuts mask 3 → 1**, so the domain edge reverts to SFINCS's default **closed wall**. Predict: Shrewsbury gauge peak **2.223 → toward obs 2.935**; HWM bias **−0.42 → toward 0**; tidal range **0.91 → toward 1.54**.
- **Production fix (also just a mask edit): ALSO activate the 137 dead wet cells** so the wall lands at the **real head of tide** (Swimming River Dam), recovering **2.47 km² of genuine upstream tidal prism** (~3.7e6 m³, ~10% of the estuary's swing). Walling at the cut *without* this slightly **under**-represents storage ⇒ expect the discriminator to be a partial fix / mild overshoot. Do the same for Shark River.
- ⚠ Levels west of the cut are coarse (mostly L1/L2 = 200/100 m). Fine for **storage**; subgrid carries the conveyance.

**STATUS: leak DIAGNOSED and measured; the FIX IS NOT YET RUN.** Stopping the leak is *predicted* to close the under-fill — that prediction is untested. Run the mask-edit discriminator on **Faber premier (`snapwave_tuned_25m`, sfincs-desktop.sif)** before believing it.

---

## ▶▶▶ (2026-07-14, EARLIER) Everything below it is a historical log; several of its "current" claims are SUPERSEDED — above all by the leak finding, which reframes the whole campaign.

### 🧨 THE HEADLINE: OUR TWO "INDEPENDENT" VALIDATION DATASETS ARE ONE DATASET, AND THE ERROR IS SPATIALLY STRUCTURED (⇒ no wave knob can fix it).

**1. FEMA MOTF IS A BATHTUB OF THE HWMs — not an independent observation.** Proven from the data, three ways: (a) **100%** of the 12 Shrewsbury/Navesink HWMs (and 100% in every other basin, within 15 m) land on MOTF *wet* pixels; (b) MOTF's flood edge (95th pct of ground it calls wet = **3.43 m**) ≈ the HWM median (**3.32 m**); (c) **a flat fill to 3.4 m reproduces MOTF with IoU 0.906.** ⇒ MOTF in this basin IS the HWM surface draped on lidar. **CONSEQUENCES: (i) MOTF and HWM are ONE line of evidence, not two — never cite them as mutual corroboration. (ii) MOTF has NO hydraulics, so POD against it STRUCTURALLY REWARDS OVER-FLOODING** — and indeed MOTF POD says Galibier beats Faber in EVERY basin (Shrewsbury 0.87 vs 0.45; south coast 0.75 vs 0.51), **directly contradicting the HWM *level* comparison. Trust the levels, not the POD.** (This vindicates the user's long-standing MOTF/CSI skepticism, and his own 2013-Street-View damage check: MOTF looks "heavy handed" in the Shrewsbury basin — houses it calls flooded show no 2013 damage/repair.) ⚠ Could NOT verify FEMA's stated methodology (WebFetch to arcgis.com is blocked) — the bathtub conclusion is inferred from the data signature alone, which is strong but should be stated as such.

**2. THE SHREWSBURY GAUGE IS A HIGH-QUALITY HWM, NOT A HYDROGRAPH.** Datum **CONFIRMED** (NWS sbin4 publishes "IN MLLW"; 11.73 ft × 0.3048 − 0.640 = **2.935 m NAVD88**). It sits at (587052, 4468841); the nearest HWM is **320 m away** (3.11 m, q=3) and neighbours at 320–900 m read 3.05–3.23 m ⇒ genuinely a separate measurement, and corroborated. **BUT the gauge DIED BEFORE THE PEAK** — 2.935 is a post-event *surveyed crest*. **No timing, no phase, no shape.** Combined with our own ±0.3 m uncalibrated boundary forcing: **the gauge can detect a 0.7 m miss but CANNOT adjudicate ±0.2 m.** Use it as one HWM among many; do not select the premier on it.

**3. ⭐ THE CENTRAL SYNTHESIS — THE ERROR IS SPATIAL, SO THE MISSING PHYSICS IS LOCAL TO THE ESTUARY.**
| observation | FABER | GALIBIER-bexp0 |
|---|---|---|
| Shrewsbury gauge crest | **−0.71** | +0.16 |
| Shrewsbury HWM (n=10) | **−0.42** | +0.06 |
| Atlantic front HWM (n=3) | +0.73 | +1.08 |
| south coast HWM (n=4) | +0.06 | **+1.37** |

**EVERY observation INSIDE the estuary favours Galibier; the OPEN COAST favours Faber. The two basins demand OPPOSITE corrections ⇒ NO global wave-setup formulation can satisfy both — which is precisely why every global knob this campaign turned came back null.** ⇒ **The missing physics must put water BEHIND the barrier WITHOUT adding setup on the open coast.** **FALSIFIABLE PREDICTION (the next test): take FABER, increase delivery across the barrier; the estuary should rise toward 2.9–3.1 m while the south-coast HWMs stay put.** (⚠ south_coast n=4 — residuals Galibier +1.50/+1.17/+2.34/+0.48 vs Faber −0.82/+0.03/+0.53/+0.53 — not an outlier artifact, but a narrow base. The Faber-premier verdict holds; state the small-n as a limitation.)

### ❌ WITHDRAWN 2026-07-14 — the "barrier eroded / no-morphodynamics" hypothesis (the 07-13 block below still pushes it; IGNORE that).
User pushback, and he is right: **Sea Bright's seawall HELD — there was no Mantoloking-style breach.** My Mantoloking analogy actually *undercut* the point. More importantly I had asserted **"the water enters OVER the barrier, not through the narrows" as a FINDING when I never MEASURED it** — crest-wetting + storage-scaling are equally consistent with inflow through the Highlands throat. **That claim is UNVERIFIED. The 07-13 block's "retroactively explains the whole wall of null results" reading rests on it and is therefore ON HOLD.**

### ⏳ THE OPEN QUESTION — WORKSTREAM J, THE FLUX PARTITION (measure the pathway instead of arguing about it)
`experiments/faber_flux_25m` = premier physics + `crsfile`/`storevel` only (`scripts/setup_flux_partition.py`). **Job 58159466 COMPLETED 07-13 18:44 (1:36:00, exit 0) — DATA IS READY, DIAGNOSIS IS NOT DONE.** Two control lines (`scripts/make_flux_crosssections.py`): a crest-following **barrier** polyline (96 vertices, y 4462500–4472000) + the **throat** at Highlands, joined at (586960, 4472000) so they SEAL the estuary. **Four placement traps found & fixed — keep them:** (a) a straight meridian at x=587050 drops INTO the estuary channel at y≈4469000; (b) raw argmax(zb) zigzags 686 m (wide developed spit) ⇒ rolling-median + ±60 m re-snap; (c) **the "wet span" at the throat MERGES TWO WATER BODIES** (bluff | estuary channel 586534–586709 | **BARRIER** 586734–586984 | ocean 587009+) — a naive line would count OCEAN flow as bay inflow; true throat ≈175 m; (d) the two lines originally INTERSECTED ⇒ double-counting.
**🚨 THE PRELIMINARY READ IS PHYSICALLY IMPOSSIBLE — DO NOT REPORT A PARTITION UNTIL IT CLOSES.** col1 Q is **NEVER positive** across four tidal cycles (range [−4039.6, −0.0]) and integrates to **−302 Mm³** into an estuary storing ~49 Mm³. A tidal channel MUST oscillate ⇒ that is a measurement bug, not a discovery. **Suspects: (1) the column↔line mapping is UNCONFIRMED — I never read a `crosssection_name` variable; if swapped, the big one-directional flow is the 9.5 km barrier line ≈0.4 m³/s per metre, which IS plausible; (2) my ~49 Mm³ storage figure is itself suspect — the box (583500–586900, 4464000–4472000) TRUNCATES the estuary (it runs south to y≈4462500 and west up the Navesink); (3) sign/normal convention.** **NEXT SESSION: (a) pin the column↔line mapping, (b) recompute estuary volume over its TRUE extent, (c) CLOSE THE MASS BALANCE (inflow through both lines = storage change + river input). If the budget does not close, say so — do not hand over a partition.**

### ✅ STILL SETTLED (unchanged from 07-13, re-verified)
PREMIER = **FABER** `snapwave_tuned_25m`, chosen on the **open-coast HWM constraint** (waves-off is already unbiased on the south coast at −0.055 ⇒ a correct wave model must barely move those HWMs; Faber +0.059, **Galibier violates by +1.4 m and is WORSE THAN NO WAVES on pooled RMSE**). The clamp (`snapwave_gammax`) is the engine-independent stability mechanism; **`niter` is INERT once clamped on BOTH engines** (the old niter "chaos" was the missing clamp) ⇒ niter=100, 4× cheaper. The subgrid **DOES** preserve the revetment: controlling `uv_zmin` sill **median 3.56 m** (IQR 3.31–3.88) and the storm tide lands almost exactly on it (Faber 3.64 ⇒ 59% overtopped; Galibier 3.88 ⇒ 75%) — **a genuine nonlinear knife-edge.** ⚠ **The old "97%/100% of crest overtopped, crest 1.95 m" census was a CELL-AVERAGE `zb` artifact — SUPERSEDED, never cite it.**

### 📌 TO CORRECT BEFORE ANYTHING IS PUBLISHED
A3's headline "estuary over-damps the tide by 0.4–0.5 m" must be revised **DOWN to ~0.3–0.4 m** (~0.11 m of the deficit is INHERITED from the under-forced 2-anchor boundary, not interior damping).

---

## ▶ (HISTORICAL) 2026-07-13 EVENING BLOCK — superseded where the 07-14 block says so (esp. the morphodynamics hypothesis and the "over-the-barrier pathway" claim).

### ✅ PREMIER = FABER (`snapwave_tuned_25m`). SETTLED — but for a COMPLETELY NEW REASON than the 07-11 verdict.
The 07-11 reasoning (Faber wins because Galibier is unstable) was WRONG and stayed retracted: the clamp is engine-independent, and `galibier_gammax2_25m` is the cleanest wave field of the campaign. **Faber wins on a DIFFERENT, stronger test — the OPEN-COAST HWM CONSTRAINT.** Official raster HWM table (`reports/hwm_basins.csv`, via `scripts/analyze_hwm_basins.py`; n: shrewsbury 10, south_coast 4, atlantic 3, shb 2):

| run | pooled bias | pooled RMSE | shrewsbury | atlantic front | **south coast** |
|---|---|---|---|---|---|
| waves OFF (both engines) | −0.375 | 0.812 | −0.606 | −0.266 | **−0.055** |
| **FABER (premier)** | **−0.090** | **0.696** | −0.422 | +0.732 | **+0.059** |
| Galibier+clamp bexp0 | +0.500 | 0.952 | **+0.058** | +1.078 | **+1.373** |
| Galibier+clamp bexp2 | +0.729 | 1.116 | +0.313 | +1.248 | **+1.656** |

**THE DISCRIMINATOR:** with waves OFF the model is ALREADY unbiased on the south coast (−0.055) — those HWMs sit inland of the beach and are satisfied by SURGE ALONE, so a correctly-calibrated wave model must barely move them. Faber obeys this (+0.059). **Galibier violates it by +1.4 to +1.7 m.** Galibier is WORSE THAN NO WAVES on pooled RMSE (0.95–1.12 vs 0.81). ⇒ **Galibier over-sets-up GLOBALLY by ~+1.4 m; its estuary "cure" (+0.058) is RIGHT FOR THE WRONG REASON** — the estuary was under-filled ~0.5 m, so a uniform over-forcing lands it near the crest while wrecking everywhere else. **The 07-13-morning "Galibier cures the estuary" reading (mesh-level bullet below) is SUPERSEDED — the distributed HWMs caught it exactly as the boundary-forcing audit warned they must (never select the premier on the single 2.935 crest).**

### ✅ CLAMP ARMS (jobs 58155140/41, both clean, 0 blowup cells)
- **ARM A — `niter` is INERT once the clamp is ON, on BOTH engines.** Clamped Galibier: niter400 **3.380** vs niter100 **3.358** (Δ0.022). Faber: 2.223 at both. ⇒ **the earlier niter CHAOS (100/200/800 collapsing non-monotonically into the blowup regime) was never a convergence property — it was the MISSING CLAMP.** Both engines are converged when clamped ⇒ **the "no converged premier" crisis is OVER, and niter=100 is fine (4× cheaper).**
- **ARM B — the lift is NOT one keyword.** Galibier+clamp bexp0 = **3.096** vs bexp2 3.380 ⇒ the Baldock exponent explains only **0.284 m** of the 1.157 m Faber↔Galibier gap. The other **0.873 m** is the rest of the v2.4.0 SnapWave rework (Roelvink et al. 2025 GMD). (bexp0-vs-Faber is now apples-to-apples: same clamp, same effective bexp, since Faber silently ignores the keyword.) **Side-finding: bexp moves the ESTUARY 0.284 m but the OPEN COAST by literally nothing (Sea Bright 3.877 in both) — it is a shallow-water/steep-bathymetry knob.**

### 🔬 MECHANISM FOUND — THE SEA BRIGHT BARRIER IS A NONLINEAR VALVE AT ITS THRESHOLD (this is the campaign's real contribution)
1. **Galibier's wave field is NOT bigger — its `hm0` is 5–15% SMALLER than Faber's everywhere** (surf zone 0.95×, barrier 0.89×, Sandy Hook Bay 0.91×). Wave setup is the gradient of RADIATION STRESS = driven by DISSIPATION, not energy ⇒ Galibier converts a *weaker* wave field into ~3× the estuary setup. Kills the "Galibier makes extra waves in the bay" hypothesis.
2. **The estuary is near-calm (mean hm0 0.28 m) — +1.7 m of setup CANNOT be generated locally.** It is IMPORTED over the Sea Bright barrier.
3. **⚠ USE THE SUBGRID `uv` FACE TABLE, NOT CELL-AVERAGE `zb`.** Cell-avg zb says the barrier crest is 1.95 m — MISLEADING. What blocks flow is `uv_zmin`; the CONTROLLING SILL (per shore-normal transect, the highest `uv_zmin` a parcel must climb) is **median 3.56 m** (IQR 3.31–3.88, weakest link 2.46 m). **⇒ THE SUBGRID DOES PRESERVE THE REVETMENT — the "missing seawall" worry is DEAD.** (My earlier "97%/100% of crest overtopped, mean crest 1.95 m" census was a cell-average artifact — SUPERSEDED, don't cite it.)
4. **THE STORM TIDE LANDS ALMOST EXACTLY ON THE SILL: ocean-side peak Faber 3.64 / Galibier 3.88 vs median sill 3.56** ⇒ overtopped length **59% (Faber) → 75% (Galibier)**. A 0.24 m setup difference swings BOTH the overtopped LENGTH and the head over it ⇒ **+40% estuary storage (49.2 → 68.9 Mm³) and +1.16 m estuary level.** The system is poised on a knife-edge — which is WHY this estuary has been so brutally hard to calibrate.
5. **⇒ THE DELIVERY PATHWAY IS OVER THE BARRIER, NOT THROUGH THE NARROWS.** This RETROACTIVELY EXPLAINS the campaign's whole wall of null results (mesh resolution +0.04 m, channel friction, the eHydro carve, the bridge_dam work): **they were all tuning the wrong door.** Those results stand as eliminations, but the shrug ("distributed conveyance near a structural ceiling") is replaced by a positive mechanism.

### 🎯 THE LEADING HYPOTHESIS FOR THE RESIDUAL UNDER-FILL — NO MORPHODYNAMICS (test this next)
Faber's wave setup is VALIDATED on the open coast (south_coast +0.059) yet it still under-fills the estuary (HWM −0.422; gauge crest −0.712). Wave setup CANNOT honestly close it (the only lever that does — Galibier — is disqualified above). With the barrier confirmed as the pathway AND confirmed to be sitting AT threshold, the suspect is the one thing SFINCS structurally cannot do: **our DEM is PRE-STORM (2010 USACE NCMP lidar) and STATIC, while the real Sea Bright barrier ERODED/overwashed as the surge peaked — progressively opening the valve. A fixed sill held at threshold for 48 h will systematically under-deliver, and NO hydraulic knob can fix it** — which is exactly the null-result pattern this campaign has produced for weeks. **CHEAP TEST: lower the barrier sill by a plausible erosion depth and see whether the estuary reaches 2.935 with FABER's well-calibrated setup and WITHOUT breaking the open coast.** If yes, the under-fill has a physical explanation instead of a shrug.

### 🔧 TOOLING FIXED TODAY
- **`validate.read_output()` (nj_sfincs/validate.py) — hydromt CANNOT open Galibier v2.4.0 maps** (`KeyError: 'crs'`). Root cause is the OPPOSITE of the obvious: **Galibier's file is the MORE CF-correct one** — it declares `grid_mapping="crs"` on its coord vars, so xugrid correctly folds crs into `ds.grid.crs` and DROPS it from data_vars; Faber omits `grid_mapping`, leaving `crs` as a loose variable, which is the ONLY reason upstream `ds["crs"].values` ever worked. Fix takes the CRS from grid → loose var → `epsg` in sfincs.inp. **Second trap: `output.set()` lazily calls `_initialize()` which calls the `read()` being replaced ⇒ must call `mod.output._initialize(skip_read=True)` first.** This unblocked the whole official raster path (all spatial metrics were silently excluding every Galibier run).
- **`plots.plot_engine_panels()` / `plot_engine_difference()` + `load_cached_floodmap()`** — read the CACHED `floodmap_hmax_lev3.tif` instead of re-downscaling. **CLIP BEFORE REPROJECTING** (the L3 raster is 6596×11300 @6.25 m; de-rotating the whole thing is MINUTES per run, clipping first is ~0.3 s). Wired into `notebooks/sfincs-nj-sandy-viz.ipynb` (cells 35–39: 4-panel engine comparison + difference map + the barrier/sill table).
- **GALIBIER BLOWUP ROOT-CAUSE FOUND IN SOURCE.** Galibier release commit `1f1d8286` changed two coupled SnapWave defaults: `snapwave_gammax` 2→999 (disables the per-sweep hard wave-height clamp Hrms≤gammax·depth) + `snapwave_baldock_exponent` 0→2 (amplified steep-coast breaking dissipation). Faber's gammax=2 clamp made it unconditionally stable; Galibier relies only on the Baldock term, which is a stiff in-iteration feedback that fails to converge on the steep Sea Bright bay mouth at niter100 with no backstop ⇒ 252 m runaway. `gammax`≠`gamma`: gamma=0.78 is the physical breaker index; gammax is a NUMERICAL safety cap (XBeach default 2, range 0.4–5), so "2 vs 999" = limiter ENGAGED vs OFF, not rival physics. Full physics in the PARAMETER PHYSICS bullet below.
- **✅ CLAMP DISCRIMINATOR RESULTS 2026-07-13 (all 3 jobs 58037182/83/84 COMPLETED CLEAN 07-11; analyzed 07-13). The 2×2 (Shrewsbury `usgs_tidal_sea_bright` peak / domain hm0 max / hm0>15 m cell-steps; obs crest 2.935):**
  - Faber, clamp ON (gammax=2), bexp=0, niter100 = `snapwave_tuned_25m`: **2.223** / 11.08 m / **0**. (niter400 arm: 2.223 / same — niter INERT on the ANSWER, though hm0 field is NOT bit-identical, max|Δhm0|=2.07 m ⇒ waves move, water level doesn't.)
  - **RUN3 `faber_gammax999_25m` (Faber, clamp OFF): 3.544 / hm0 max 16,247 m / 8,067 blowup cell-steps ⇒ FABER EXPLODES WITHOUT THE CLAMP.** ⇒ **THE CLAMP IS THE STABILITY MECHANISM, ENGINE-INDEPENDENT** — Faber's celebrated stability was never solver quality, it was the `gammax=2` crutch. Fully confirms the 07-11 source-level root cause. Its estuary "lift" to 3.544 rides on a garbage wave field ⇒ spurious, don't cite as physics.
  - **RUN1 `galibier_gammax2_25m` (Galibier niter400, clamp RESTORED): 3.380 / hm0 max 9.80 m / 0 blowup cells — THE CLEANEST WAVE FIELD OF THE CAMPAIGN (beats Faber's 11.08).** Both gauges spike-free (0 jumps >0.5 m/10 min), peak at the TRUE surge time 2012-10-30T01:10. Residual zs hot spot = just **9 cells** in one 250×100 m patch of Sandy Hook Bay (589583–589834, 4482680–4482778), sub-hourly (invisible in hourly snapshots), **ZERO in the estuary** — vs galibier_niter400's 894 cells / 1297 m. Estuary-box max depth 19.24 m == Faber's 19.24 m exactly. ⇒ **THE LIFT DID NOT COLLAPSE ⇒ it is REAL Galibier wave-setup physics, not the missing clamp. Lands on the pre-declared "stays ~3.2 clean ⇒ stable improved-premier candidate" branch. RETRACTS the 07-11 Faber-premier verdict.**
  - **RUN2 `faber_bexp2_25m`: INCONCLUSIVE EXACTLY AS PRE-DECLARED — its hm0 field is BIT-IDENTICAL to `snapwave_tuned_25m` (max|Δ|=0.0) ⇒ the Faber v2.3.3 binary SILENTLY IGNORES `snapwave_baldock_exponent` (added-in-Galibier keyword; no warning in the log).** It is just a re-run of stock Faber. **NOT a null — do not log it as one.** (The pre-declared byte-diff check is what caught this; keep pre-declaring such checks. ⇒ bexp CANNOT be tested on Faber at all; test it on Galibier instead — see next-steps.)
- **❌ WITHDRAWN 2026-07-13 — "Galibier over-sets-up on the ocean side" WAS MY COUNTER-EVIDENCE AND IT DOES NOT SURVIVE RE-DERIVATION. The Sea Bright storm-tide "obs 3.47" IS NOT A SINGLE NUMBER.** Re-derived from source (`data/gtsm/sandy_storm_tide_nj.nc`, built by `scripts/download_sandy_storm_tide_sensors.py` from USGS STN rapid-deployment SSS sensors; `stormtide_m` = 30-min low-pass still-water+setup = the right series to compare to model `zs`): there are **TWO CO-LOCATED SENSORS AT THE IDENTICAL COORD (−73.9730, 40.3722)** — **2258 (SSS-NJ-MON-002WV): still-water peak 3.465 m @ 00:12** and **2259 (SSS-NJ-MON-003WV): still-water peak 4.973 m @ 00:30**. **They disagree by 1.51 m at the same point.** (2259's still-water 4.97 is suspiciously near its own wavemax 5.94 ⇒ likely surf/overwash-contaminated or a datum/mount-elevation error; 2258 is the more credible one, but this is NOT established.) Model Faber **3.643** and galibier_gammax2 **3.877** BOTH sit comfortably INSIDE the 3.47–4.97 observational spread ⇒ **this gauge CANNOT arbitrate the premier, and the "+0.17 vs +0.41 ocean-side overshoot" argument I built on 3.47 is VOID.** [[project_bridge_dam]]'s "Sea Bright 3.64 vs obs 3.47, boundary exonerated" quietly picked ONE of the two sensors without saying so — that framing needs the caveat. **⇒ Only the ENGINE DIFFERENTIAL (+0.23 m ocean vs +1.16 m estuary) remains usable; every absolute claim at this gauge is inside the noise.**
- **PHASE-LAG HYPOTHESIS TESTED 2026-07-13 (user's observation) — NOT CONFIRMED IN THE DATA WE HAVE, but UNTESTABLE where it would matter most.** User's mechanism is sound in principle: the boundary anchors peak at DIFFERENT times (**Atlantic City 2012-10-30T00:00, The Battery 01:00** — Battery is 1 h later), and Sandy Hook's latitude interpolates ~83% toward the Battery ⇒ the model should inherit Battery's late timing at Sandy Hook. **BUT empirically: best lag = 0 h on BOTH the full 48-h window (RMSE 0.142) AND the surge/rising-limb window (RMSE 0.128); shifting the model EITHER direction strictly worsens RMSE (−1 h → 0.351, +1 h → 0.289).** The hourly table shows the model SHADOWING obs in timing (tide peaks/troughs align at 13:00, 18:00, and on the final limb) with a **steady −0.10 to −0.16 m AMPLITUDE bias**, not a shift. **The likely source of the visual impression: on the final limb obs rises at +0.494 m/h, so a −0.11 m low bias is visually near-indistinguishable from a ~13-min rightward shift** — a downward-shifted curve on a steep monotone rise LOOKS delayed. **⚠ CRUCIAL CAVEAT: the Sandy Hook gauge DIED at 10-29T23:00, BEFORE the surge peak — so the phase of the surge PEAK at Sandy Hook is UNVERIFIABLE, and that is exactly where the Battery-weighting would bite hardest. The hypothesis is plausible, mechanistically supported by the 1-h anchor-peak offset, and simply cannot be tested with the available record. State it as a limitation, don't claim it either way.** **BONUS: the steady −0.10/−0.16 m bias is an AMPLITUDE deficit, INDEPENDENTLY corroborating the 2-anchor under-forcing finding** (model is pinned to a boundary prescribed ~0.25 m too low at that latitude). (NOTE: validate.py's A5 `−0.31 m` prefail error is from the 12.5 m `snapwave_tuned`; these −0.10/−0.16 numbers are the 25 m premier — different runs, not a contradiction.)
- **📌 USER'S STRATEGIC POINT (2026-07-13, ENDORSE IT): Sandy has too few trustworthy ground truths to arbitrate a ±0.3 m question.** Sandy Hook gauge died pre-peak; the Shrewsbury crest is a single surveyed number with no hydrograph; the Sea Bright storm-tide "obs" is two co-located sensors 1.5 m apart; HWMs are sparse and basin-mixed. **⇒ USER PROPOSES EXTENDING THE ANALYSIS TO A MORE RECENT NOR'EASTER with better data coverage. Strongly agree — this is the right long-game move**: a modern event would have surviving gauges through the peak, denser sensors, and a real hydrograph at the Shrewsbury, which is the ONLY way to settle the absolute-level questions this campaign keeps bouncing off. Frame Sandy as the mechanism/differential study (where our results ARE solid) and the nor'easter as the validation event.
- **🚨🚨 BOUNDARY-FORCING AUDIT 2026-07-13 (user raised it; it CORRECTS TWO ERRORS I had been repeating, and it changes HOW WE PICK THE PREMIER).** Verified directly from `experiments/snapwave_tuned_25m/sfincs_netbndbzsbzifile.nc` + `data/gtsm/noaa_sandy_validation.nc`.
  - **❌ RETRACTION 1 — "Sandy Hook obs peak ≈3.42 m" IS WRONG AND I HAD IT IN MEMORY. 3.417 is THE BATTERY's peak.** The Sandy Hook NOAA gauge (8531680) **DIED 2012-10-29T23:00 ON THE RISING LIMB at 2.808 m**, two hours before the Battery peaked ⇒ **there is NO observed Sandy Hook peak, and every "model under-predicts Sandy Hook by 0.2–0.3 m" claim built on 3.42 is VOID.** (validate.py's A5 already had this right — `gauge_obs_peak_m` is the TRUNCATED pre-failure value; I misquoted it. Use `gauge_peak_err_prefail_m` = **−0.31 m** = model 2.50 vs obs 2.81 at the last valid sample, an honest same-time comparison, though it sits on a steep limb so phase error inflates it.)
  - **❌ RETRACTION 2 — `plot_surge`'s scary "max" line is a PLOTTING artifact, not the forcing.** `nj_sfincs/plots.py`:111 draws `bzs.max(pt)` explicitly labelled "max over boundary" as a summary curve. We are **NOT** applying max(Battery, AC) along the coast. `bzs` has 2 stations with distinct series and SFINCS interpolates between them.
  - **✅ BUT THE REAL BIAS IS THE OPPOSITE OF "OVER-FORCING": the 2-anchor ramp UNDER-forces the north Jersey shore.** Only **2 Dirichlet anchors, 149,655 m apart**: S = Atlantic City (y=4,356,334, peak **1.877**), N = The Battery (y=4,505,989, peak **3.417**). Linear interp prescribes **3.150 m at Sandy Hook's latitude** (y=4,480,052) — i.e. 0.27 m BELOW the Battery — whereas Sandy's surge was sharply peaked in the NY Bight apex and Sandy Hook (only 26 km from the Battery) really saw ~Battery levels. **The model's Sandy Hook peak 3.154 ≈ the prescribed 3.150 ⇒ it is simply PINNED to the boundary, not "failing".** ⇒ the north end of the open boundary is under-forced by roughly ~0.25 m. Confirms the 07-08 "SEPARATE real improvement exposed" note and extends it to Sandy Hook itself.
  - **✅ USER'S 10 m-DEPTH POINT IS ALSO REAL, and pushes the OTHER way:** we prescribe coastally-MEASURED gauge levels at the ~10 m depth contour, but a coastal gauge already contains local shoaling/setup ⇒ the model shoals it a second time = double-counting = over-forcing. **⇒ TWO BIASES OF OPPOSITE SIGN (coarse-ramp under-forcing ~0.25 m vs depth-placement over-forcing), net sign UNKNOWN, magnitude ~0.3 m either way. The forcing is uncalibrated at the ±0.3 m level — DO NOT tune to it.**
  - **🔑 THE DECISIVE CONSEQUENCE (this is the useful part): the boundary forcing is IDENTICAL in every engine run** (already verified 07-10: max zs on msk==2 cells = 3.192 m in all 8 runs) ⇒ **forcing bias is COMMON-MODE and CANCELS EXACTLY in every A/B differential.** ⇒ **DIFFERENTIALS ARE ROBUST** (wave setup 1.16 m; wind null +0.002; narrows over-dredge +0.81; wall +0.60) **while ABSOLUTE gauge match is CONTAMINATED.** ⇒ **DO NOT SELECT THE PREMIER BY WHICH ENGINE LANDS CLOSEST TO THE 2.935 CREST — that criterion is invalid at the ±0.3 m level.** Select on NUMERICS/PHYSICS (stability, convergence, mechanism: clamped-and-converged beats accidentally-clean) and report wave setup as a DIFFERENTIAL against the I-3 no-wave baseline. **This is USER-ENDORSED framing ("I'm not super worried if our gauge comparisons overshoot a bit") and it RESCUES the campaign's logic — every differential result survives; only the fragile absolute "under-fill −0.71" reading depends on the forcing.**
  - **⚠ AND IT DENTS A3, our "safest" headline — quantify before publishing.** The boundary also under-delivers **TIDE** at Sandy Hook's latitude: prescribed pre-storm range **1.675 m vs OBSERVED 1.824 m = −0.15 m** (Battery 1.725 and AC 1.439 are exact by construction — they ARE the anchors; Cape May is −0.28 m, outside the ramp). The estuary transmits ~75% of the coastal tide (obs Shrewsbury 1.37 / obs Sandy Hook 1.82), so ~**0.11 m** of the interior tidal deficit is INHERITED FROM THE BOUNDARY, not estuary damping. ⇒ **A3's "estuary over-damps the tide by 0.4–0.5 m" should be REVISED DOWN to ~0.3–0.4 m of genuine interior damping.** The finding SURVIVES (still the most robust yardstick, still version-independent) but the headline number must be corrected before it goes in the report.
  - **FOLLOW-UPS:** (a) add an intermediate boundary anchor / non-linear N–S ramp — but note Sandy Hook CANNOT be a forcing point (gauge failed pre-peak; that's why it was demoted to validation-only), so this needs a reconstruction, not just another gauge; (b) re-derive the Sea Bright storm-tide obs (≈3.47) from source — it is the weakest number still load-bearing; (c) quantify the depth-placement over-forcing (compare prescribed level at the 10 m contour vs an offshore reanalysis/GTSM level).
- **⏳ IN FLIGHT (4 wave-setup-attribution arms, submitted 2026-07-13, RUNNING at 64 cores, -t 3h, main-redhat preemptible → resubmit if they die). `scripts/setup_wavesetup_attribution.py` stages all four from `snapwave_tuned_25m`; each verified ONE-knob by inp diff; containers verified at start.** `hpc/sfincs_run.slurm` bumped `-c 48→64`, `--mem 48→64G` (352 of 486 nodes have 64 cores). **A `galibier_gammax2_niter100_25m` (job 58149715, Galibier sif) — is the CLAMPED Galibier converged in niter? A/B vs galibier_gammax2_25m (3.380); same ⇒ stable+converged premier candidate, 4× cheaper.** **B `galibier_gammax2_bexp0_25m` (job 58149716, Galibier sif, gammax=2+niter400+bexp=0) — isolates the Baldock exponent on a stable clamped solve (the test RUN2 couldn't do since Faber ignores the keyword); drops→~2.2 ⇒ lift IS the Baldock exponent (citable); stays ~3.4 ⇒ lift is the REST of the Roelvink-2025 solver rework, no single keyword explains it.** **C `faber_nowaves_25m` (job 58149718, FABER sif, snapwave=0) + D `galibier_nowaves_25m` (job 58149717, Galibier sif, snapwave=0, theta=1.0) = WORKSTREAM I-3, wind drag STAYS on so this isolates WAVE setup: C≈D ⇒ the ENTIRE 1.16 m engine gap is wave setup (proven, not inferred) + gives both engines a shared no-wave baseline to measure setup against; C≠D ⇒ something OUTSIDE SnapWave changed between versions and every version A/B in the campaign is confounded.** Waves-off arms are cheap (~15–25 min; SnapWave was ~85% of runtime). **Analysis recipe unchanged:** his peaks (`usgs_tidal_sea_bright`=Shrewsbury, `sandy_hook`, `usgs_stormtide_sea_bright`) + hm0>15 m census + zsmax-depth cells exceeding Faber's real 30.19 m deep-channel ceiling (NOT a naive >20 m threshold — a genuine 27 m navigation channel at (583521,4481725) is wet in every run and trips it); env `micromamba/envs/sfincs/bin/python`; DON'T loop V.evaluate(). **NOTE Faber maps (v2.3.3) have NO `mesh2d_face_x/y` — derive face centres from `mesh2d_face_nodes` + node coords (Galibier maps do have them).**
- **✅✅ WORKSTREAM I-3 ANSWERED 2026-07-13 — DECISIVE, AND IT VALIDATES EVERY VERSION A/B IN THE CAMPAIGN. `faber_nowaves_25m` and `galibier_nowaves_25m` (snapwave=0) give IDENTICAL gauge peaks TO 3 DECIMALS: Shrewsbury 1.663 / Sandy Hook 3.129 / Sea Bright 3.026.** ⇒ **the SFINCS barotropic core is UNCHANGED between v2.3.3 Faber and v2.4.0 Galibier ⇒ the ENTIRE engine difference is WAVE SETUP (proven, not inferred), and no version A/B in this campaign is confounded by a non-wave change.** **WAVE-SETUP DECOMPOSITION at the Shrewsbury gauge (peak − no-wave baseline 1.663):** Faber(clamp on) **+0.560** | galibier_niter400(unclamped) +1.572 | **galibier_gammax2(clamped, CLEAN) +1.717** | faber_gammax999(unclamped, garbage waves) +1.881 | **implied by OBS crest 2.935 → +1.272** (⚠ absolute, so forcing-bias-contaminated — treat as indicative only). ⇒ **modelled estuary wave setup differs by a FACTOR OF ~3 between engines (0.56 vs 1.72 m) and the observation sits BETWEEN them.** **🔬 THE NEW PHYSICS QUESTION — AN ESTUARY/OCEAN TRANSFER ASYMMETRY:** ocean-front (Sea Bright, baseline 3.026) setup is Faber **+0.617** vs Galibier-clamped **+0.851** (only 1.38×), while the ESTUARY setup is +0.560 vs +1.717 (3.07×). So estuary-setup/ocean-setup ratio = **0.91 (Faber) vs 2.02 (Galibier)** — under Faber the estuary rise ≈ the ocean setup; under Galibier it is DOUBLE it. Sheltered back-estuary "setup" exceeding open-coast setup is physically odd for LOCAL setup (fetch-limited behind the barrier) — likely a filling/volume effect (elevated coastal level sustains inflow) — but the 2× transfer ratio is the thing to explain and is the strongest remaining reason to doubt Galibier. **NEXT DIAGNOSTIC: compare the hm0 field near/inside the estuary between `snapwave_tuned_25m` and `galibier_gammax2_25m` — is Galibier generating extra wave energy INSIDE the bay/estuary (snapwave_wind=1 local growth), or only at the bay mouth?** (Sandy Hook wave setup is negligible in both: +0.025 Faber / +0.067 Galibier — the sheltered bay behaves as expected.)
- **🎯 A2 HWM BASIN RE-TABULATION 2026-07-13 — INDEPENDENT CORROBORATION THAT GALIBIER-CLAMPED FIXES THE ESTUARY, *BUT* IT WRECKS THE OPEN COAST. This is the sharpest premier evidence yet and it is NOT a clean win for either engine.** Computed DIRECTLY ON THE MESH (`scratchpad/hwm_mesh.py`) because hydromt's `load_floodmap` is unusable here: it must WRITE a floodmap tif (quota) **and it CANNOT OPEN GALIBIER v2.4.0 MAPS AT ALL — they carry no `crs` variable** (`KeyError: 'crs'`; Faber maps have it). Mesh method mirrors `hwm_metrics` (50 m radius, ground filter zb≤obs+0.5, wet≥DEPTH_MIN=0.15) but samples 25 m FACES not downscaled L3 pixels ⇒ **absolute biases are METHOD-SHIFTED vs the published raster A2 (Faber shrewsbury reads −0.554 here vs −0.27 by raster) — QUOTE ONLY THE RUN-TO-RUN DELTAS, not these absolutes.** **HWM bias by basin (q≤2, + = model too HIGH):**
  | run | pooled(19) | atlantic_oceanfront(3) | **shrewsbury_navesink(10)** | sandy_hook_bay(2) | south_coast(4) |
  |---|---|---|---|---|---|
  | `snapwave_tuned_25m` (Faber, clamp on) | −0.45 | −0.23 | **−0.554** (rmse 0.637) | −0.29 | −0.41 |
  | `galibier_niter400_25m` (clamp OFF) | +0.29 | +0.46 | **−0.013** (rmse 0.208) | −0.27 | **+1.20** |
  | **`galibier_gammax2_25m` (CLAMP ON, the candidate)** | +0.31 | +0.41 | **+0.088** (rmse 0.261) | −0.21 | **+1.06** |
  | `faber_gammax999_25m` (clamp off, garbage waves) | +0.04 | +0.28 | −0.344 | −0.27 | +0.99 |
  - **✅ THE ESTUARY VERDICT: Galibier-clamped essentially CURES the behind-barrier under-fill** — Shrewsbury/Navesink bias −0.554 → **+0.088** and RMSE 0.637 → **0.261 (2.4× better)** across 10 spatially-distributed marks. **This is a SECOND, INDEPENDENT observation type (10 HWMs) agreeing with the Shrewsbury gauge crest** that the estuary under-fills on Faber and fills correctly on Galibier-clamped. Strong.
  - **🚨 THE CATCH — IT IS NOT UNIFORMLY BETTER, IT REDISTRIBUTES THE ERROR: Galibier OVERSHOOTS THE SOUTH COAST BY ~+1.1 m** (south_coast −0.41 → **+1.06**, n=4, tight std 0.41; and atlantic_oceanfront −0.23 → +0.41). The south_coast basin (y<4,458,000 = Shark River/Belmar/Avon) is directly wave-exposed open Atlantic ⇒ **Galibier's extra wave setup is far TOO STRONG on the open coast while being ~right in the sheltered estuary.** A setup-formulation change should bias CONSISTENTLY; instead it is +1.1 m on the open coast and +0.09 in the estuary. **⇒ live worry: Galibier-clamped may be getting the estuary RIGHT FOR THE WRONG REASON — the estuary fills because the open-coast level driving it is over-set-up.** This is the same "over-sets-up on the ocean side" concern that the (broken, 1.5-m-spread) Sea Bright storm-tide sensor could not test — and now a proper DISTRIBUTED dataset DOES test it, and it flags Galibier. **⇒ NEITHER ENGINE IS A CLEAN PREMIER: Faber under-fills everywhere (pooled −0.45); Galibier fixes the estuary but over-floods the open coast (pooled +0.31). This is now the central premier tension.** NOTE `faber_gammax999` (garbage waves) ALSO lifts south_coast to +0.99 ⇒ the open-coast over-setup tracks the CLAMP/wave-energy, not the Baldock exponent per se — the pending `galibier_gammax2_bexp0` arm should discriminate.
- **🐛 SFINCS BUG FOUND 2026-07-13 (BOTH ENGINES — add to the Deltares report): `snapwave=0` + `storefw=1`/`storewavdir=1` ⇒ SIGSEGV AT FINALISATION.** The solve COMPLETES normally (full 72 h, "Closing off SFINCS", timing summary printed), then SFINCS segfaults writing the wave-friction/wave-direction output fields, which were never allocated because the wave solver was off. **Consequence: the crash happens during netCDF close, so `sfincs_map.nc` is left TRUNCATED (26 of 73 steps) while `sfincs_his.nc` — already flushed — SURVIVES INTACT AND COMPLETE (433/433).** sacct says FAILED even though the physics is fine ⇒ **don't discard a "FAILED" waves-off run; check the his file first.** Identical on v2.3.3 Faber and v2.4.0 Galibier ⇒ upstream, long-standing. **FIX: set `storefw=0` + `storewavdir=0` whenever `snapwave=0`** (now baked into `scripts/setup_wavesetup_attribution.py`; the two arms were re-staged + resubmitted as jobs 58153158/59 purely to recover the MAP files — the I-3 gauge result above is already final from the salvaged his files, backed up in scratchpad).
- **NEXT / OPEN (2026-07-13 queue). Two CHEAP one-knob runs finish the story — both off `galibier_gammax2_25m` (stable + clean = the new reference arm):** (1) **`galibier_gammax2` + `snapwave_niter=100`** — is the CLAMPED Galibier converged w.r.t. niter the way Faber is? Same answer ⇒ genuinely stable+converged config ⇒ a real premier candidate AND 4× cheaper. (2) **`galibier_gammax2` + `snapwave_baldock_exponent=0`** — isolates the Baldock exponent on a stable clamped solve (the test RUN2 was *supposed* to do but couldn't, since Faber can't read the keyword). Drops to ~2.2 ⇒ the lift IS the Baldock exponent; stays ~3.4 ⇒ the lift is the rest of the Roelvink-2025 GMD solver rework. (3) Workstream **I-3 waves ON/OFF at the estuary** on BOTH engines — now the highest-value untouched test: if both engines agree with SnapWave off, the ENTIRE 1.16 m gap is wave setup (near-certain, but it's the clean proof). (4) THEN pick the premier and re-tabulate A1/A2. (5) Deltares report — now BETTER grounded: shipped default (gammax=999 + niter100) is unstable on steep coasts, and `gammax=2` restores a clean wave field at no cost. (6) Report `reports/shrewsbury_investigation.md` I/A/premier + §E/F/H still need the update (do it AFTER the premier settles — it has already churned twice).
- **GIT (user commits; git NOT on the batch-shell PATH this session so Claude couldn't `git add`).** NEW this session: `scripts/setup_clamp_experiments.py`. MODIFIED: `hpc/sfincs_run.slurm` (-c 24→48, --mem 24→48G, guarded `numactl --interleave=all`). Plus everything from the 07-09/10 handoff still to commit (report + scripts/setup_{e,i,h2,faber_niter400}.py — see GIT STATE bullet below). `data/` + `experiments/` are gitignored (regenerable from scripts).

---

2026-07-08. Advisor gave new ideas; re-opening the Shrewsbury under-fill after the 07-05 "investigation complete / accept snapwave_tuned + write up" verdict ([[project_bridge_dam]]). User skeptical of MOTF/CSI as a verdict; wants the HWM+gauge yardsticks proven apples-to-apples FIRST, then a boundary experiment. Working through the plan conversationally; user will flip to plan mode at end. NOTHING run/edited yet this session — audit only.

**DELIVERABLE 1 — HWM/gauge apples-to-apples audit (AGREED, not yet done):**
- Datums verified CLEAN: `noaa_sandy_validation.nc` is `datum=NAVD88, units=m` (4 stations 8518750/8531680/8534720/8536110); model WSE NAVD88; USGS HWMs filtered to vdatum_id==2 (NAVD88), ft→m. Sandy Hook gauge comparison IS apples-to-apples.
- HWM inventory: `data/validation/sandy_hwms.geojson`, 31 marks, elev 2.9–5.79 m, quality {1:2,2:17,3:7,4:5}; headline metric restricts to q≤2. 16 marks fall in Shrewsbury/Navesink bbox (-74.10..-73.95, 40.33..40.42) — but that bbox MIXES ocean-front (id6163/6150 on Sea Bright barrier, surge delivered fine) with behind-the-barrier Highlands/Navesink (id6143/6145) → pooled RMSE blurs the part we get right with the part we don't.
- THREE concrete sub-deliverables: (a) **re-derive the Shrewsbury crest datum from source** — `shrewsbury_gauge_peak` in validate.py hardcodes 11.73 ft MLLW→2.935 m NAVD88 (VDatum geoid18, MLLW 0.640 m below NAVD88); only un-cross-checked datum conversion; if NWS "11.73 ft" is gauge-datum/MHHW not MLLW the −0.55 m in-river bias shifts. (b) **partition HWMs by HYDRAULIC BASIN** (ocean-front vs behind-barrier), not lon/lat box, and re-tabulate residuals per basin — this is what turns HWMs into a real conveyance verdict. (c) **add a reproducible tidal-RANGE metric** to validate.py (currently only peak error exists; the "interior range 0.91 vs obs 1.54 = over-damped" number is a one-off notebook figure). Also note the gauge point is nudged 21 m into the −4.2 m channel cell ([[project_bridge_dam]] model.py:144) — state explicitly when reporting.
- HWM matching in `hwm_metrics`: 50 m search radius, filter `ground ≤ obs+0.5`; risk that 50 m lets a building-lot mark grab a channel-cell WSE, inflating agreement in the narrow cells we care about.

**DELIVERABLE 2 — N/NW boundary "wall" experiment (advisor's idea). Boundary-forcing AUDIT DONE this session:**
- Hypothesis: N/NW (Raritan Bay / harbor) waterlevel boundaries let surge water ESCAPE that would otherwise fill the bay + push up Navesink/Shrewsbury. Advisor proposes removing them (NW → wall) as a diagnostic, even though unphysical.
- FORCING AUDIT (the key finding): forcing dataset `data/gtsm/noaa_sandy_nj.nc` = only 3 stations (Battery 8518750 peak 3.42, Atlantic City 8534720 peak 1.88, Cape May 8536110 peak 1.75). **Sandy Hook 8531680 is NOT a forcing point** — failed mid-storm (~48/96), demoted to validation-only. Built `model/` has **exactly 2 Dirichlet bnd anchor points** (`sf.water_level.data` bzs, index 0/1): NORTH (583282, 4505989)=Battery peak 3.42; SOUTH (550135, 4356334)=AtlanticCity peak 1.88. SFINCS interpolates the water surface between just these two across every msk=2 cell.
- CONCLUSION: the N/NW boundary is driven by Battery @3.42 m — and that's the CORRECT observed bay level (Sandy Hook validation also peaked ~3.42; Battery≈Sandy Hook, real Raritan Bay peak ~3.4 near-uniform). **Bay is NOT under-forced; boundary pins it to reality.** A Dirichlet boundary at observed 3.4 can't strongly drain an interior that's already BELOW 3.4 (under-filled) — physically it feeds water IN. So the coherent version of the advisor's hypothesis is a ROUTING/volume escape-hatch (open boundary = low-resistance exit reducing head buildup), NOT a level-clamp sink.
- **REFRAME the experiment (user liked this):** walling the N/NW can only RAISE the bay ABOVE 3.4 m, contradicting Sandy Hook obs. So read the result as "how much UNPHYSICAL bay super-elevation would it take to fill the rivers" = it QUANTIFIES the conveyance deficit, does not cure it. Expect SMALL effect on the rivers (Sandy Hook + Sea Bright storm-tide both validate on the seaward side of the Rumson–Sea Bright narrows → head at narrows entrance already ~correct → deficit is landward conveyance, consistent with 07-05). If wall shows LARGE effect, it's a compensating error masking conveyance. Cheap: boundary-TYPE change → NO subgrid rebuild, reuses frozen mesh.
- Caveats for the experiment: (1) walling conflates removing an artificial escape with removing REAL harbor-surge inflow (NY Harbor surge genuinely entered via the Narrows) → a level drop could be "lost real inflow" not "stopped leak" → measure NET FLUX / mass budget across the boundary, not just levels. (2) `west_below_bay` (fx<582500 & fy<4474000) is already OUTFLOW (radiating) not waterlevel — a bigger "escape" suspect than the Dirichlet cells, but it's SW mainland far from the rivers. (3) hard wall risks reflection/seiche.
- **SEPARATE real improvement exposed:** 2 anchors over a huge N–S gradient (3.42→1.88 over ~150 km) with Sandy Hook removed → the SOUTHERN ocean edge (Shark River latitude) is pulled toward AC 1.88 and may be UNDER-forced there (irrelevant to Navesink/Shrewsbury, relevant to Shark River gauge). Adding an intermediate anchor is a defensible fix independent of the wall test.

**WORKSTREAM B — DONE 2026-07-08 (subgrid-conveyance diagnostics, current build, nothing rebuilt). VERDICT: subgrid conveyance is NOT under-built — decisive.** Reproducible probe committed: `scripts/probe_subgrid_conveyance.py` (reads any experiment's `sfincs.nc`+`sfincs_subgrid.nc`). Key mechanics: exactly reconstructed hydromt_sfincs' uv-point ordering (iterate faces, append mu1[,mu2],nu1[,nu2]; validated 1,355,381==npuv) → maps each `uv_havg/uv_nrep/uv_pwet` row to a physical location; **subgrid table has 10 levels** (NOT the 5 mesh-refinement levels), `weight_option="min"` (havg = conveyance depth of the LOWER-flux side), `nr_subgrid_pixels=8`. Findings: (B1) **on the surveyed eHydro thalweg (38,847 finest-level uv-pts), effective flow depth SFINCS uses = (zs−bed) at ratio 1.00 for zs=0..3.4 m, nrep≈0.017 (clean channel)** — conveyance faithful at all surge levels. Deep-channel `zmax` is deeply negative (median −2.8 m) so at Sandy surge SFINCS extrapolates hu=havg_top+(zs−zmax) → healthy ~6 m depth. (carve fidelity: median uv_zmin−eHydro_bed=−0.03 m — confirms [[project_bridge_dam]] carve is IN the subgrid.) (B2) havg semantics = "min"-flux-side conveyance depth, 10-level. (B3) interior roughness physical: channel-channel nrep 0.017, marsh-marsh 0.038 — NO over-damping blend. (B4) narrows well-resolved (stems 150–230 m = 12–18 cells at 12.5 m); bay↔estuary throat has NO shallow sill (min bed ≤−9.6 m, scoured to −13 m; hundreds of deep conveying cells per transect). Sinuosity logically RULED OUT as an under-fill cause (grid straightens the path → LESS friction → would OVER-fill). ⇒ **B5 eHydro widen/deepen rebuild NOT warranted.** Result STRENGTHENS the 07-05 "distributed/structural, not a fixable knob" verdict with direct subgrid evidence.
- **NEW finding (feeds Workstream A, important):** the interior validation gauges snap to DRY HIGH-GROUND cells — `sfincs_his.nc` point_zb = +1.38 (tidal_sea_bright), +1.25 (shark_river), +2.03 (stormtide_sea_bright); their modeled "flat" pre-storm zs is just zs≡zb (SFINCS reports bed for dry cells). So the **"interior tidal range 0.91 vs obs 1.54 = over-damped" metric is a gauge-placement ARTIFACT** (computed on a dry bank cell) and must be re-sampled at a genuinely-wet channel cell. Sanity check from `sfincs_map.nc`: a real wet channel cell mid-Shrewsbury (bed −2.11, wet 100%) tides 0.89 m vs bay 1.51 m → there IS real up-estuary tidal damping, but it must be quantified with the proper Workstream-A tidal-range function on wet cells, not the his gauges. (Map NaN-masks dry cells; the nudged gauge cell is finite only 53% of the time.)

**WORKSTREAM A — DONE 2026-07-08 (validation apples-to-apples, all in `nj_sfincs/validate.py`, current build). Verified end-to-end on `snapwave_tuned` via `evaluate()`.**
- **A1 crest datum RESOLVED = MLLW (confirmed, not assumed).** NWS/NOAA gauge for USGS 01407600 is `sbin4`, published "(IN MLLW)" (water.noaa.gov/gauges/sbin4); USGS NWIS param 72279 is a SEPARATE NAVD88 feed. So "11.73 ft" is MLLW → 11.73×0.3048−0.640 (VDatum MLLW-below-NAVD88) = **2.935 m NAVD88 STANDS**, and the −0.67 m Shrewsbury deficit stands. (Only un-cross-checked link now = the 0.640 m VDatum offset value itself.) Codified as SHREWSBURY_CREST_FT/…_MLLW_BELOW_NAVD88_M + CREST_DATUM_NOTE.
- **A2 HWM basin partition = the real conveyance verdict.** `classify_hwm_basin()` — 4 basins by UTM-18N coordinate thresholds (per simple-geometry pref, not polygons): sloped Sea Bright/Monmouth barrier axis `x>586000+0.075*(y−4456000)`, `y>4474000`=sandy_hook_bay, `y<4458000`=south_coast, else shrewsbury_navesink. **RESULT: pooled HWM bias −0.05 m is a MIRAGE** — it averages atlantic_oceanfront **+0.48** (n3, wave-exposed, model slightly high), sandy_hook_bay **+0.03** (n2), south_coast **+0.06** (n4), and **shrewsbury_navesink −0.27 m (n=10, the ONLY negative basin)**. The behind-barrier estuary is the sole under-fill → HWMs now independently corroborate the conveyance deficit that pooled RMSE hid. hwm_metrics() emits per-basin n/bias/rmse.
- **A3 tidal-range metric (new `tidal_range_metric()`) — over-damping is REAL.** Samples modeled range at genuine WET channel cells near each gauge's TRUE coord from `sfincs_map.nc` (NOT the dry his points), obs from `usgs_sandy_tidal_nj` (01407600/01407770), SAME first-24h pre-storm window. Shrewsbury **mod 0.84 vs obs 1.37 → −0.53 m damping**; Shark R **1.27 vs 1.82 → −0.55 m**. So the model genuinely over-damps the tide ~0.5 m even though B proved deep-channel conveyance is faithful → the residual is DISTRIBUTED intertidal/geometric damping, not a carve/roughness input error. (Caveat coded: hourly map mildly under-samples the range; effect ≪ the 0.5 m gap.)
- **A4 gauge-nudge reported.** shrewsbury_gauge_peak now emits `shrewsbury_his_cell_zb_m` (+1.38 = the bank cell SFINCS snapped to despite the 21 m nudge) and a note: the bank bed corrupts the tidal RANGE (→ use A3 channel cells) but NOT the surge PEAK (local water surface continuous at peak; his is 10-min so captures it) → modeled peak stays 2.265, deficit −0.67.
- **A5 Sandy Hook truncation framing fixed.** gauge_peak_error now returns `gauge_mod_peak_prefail_m` 2.50 / `gauge_peak_err_prefail_m` −0.31 (honest, gauge died 10-29 23:00 rising limb) AND `gauge_mod_peak_full_m` **3.149** (true model peak the dead gauge never saw). Don't read −0.31 as model error.
- **Net:** three independent yardsticks now AGREE and are defensible: gauge crest (−0.67), basin-partitioned HWM (−0.27 estuary-only), tidal range (−0.53). The under-fill is real, localized to the behind-barrier estuary, and (per B) NOT a fixable subgrid knob. Staged (not committed): validate.py + scripts/probe_subgrid_conveyance.py. Remaining plan: C (wall/flux), D (wind), then F→E, G anytime.

**WORKSTREAM C — STARTED 2026-07-08 (NW boundary flux/wall bracket, current build, NO subgrid rebuild). Runs SUBMITTED + RUNNING, analysis pending.**
- Setup `scripts/setup_c_wall.py`: derives 2 runs from the ALREADY-BUILT `experiments/snapwave_tuned` inputs (copies mesh/subgrid/forcing/snapwave, NO rebuild) → `experiments/nw_open` (snapwave_tuned + `storevel=1`, open-boundary baseline) and `experiments/nw_wall` (+ N/NW boundary CLOSED: mask 2→0 at northing>4,480,000 = the Raritan Bay/lower-harbor edge; 688 cells walled; Atlantic surge edge + interior + subgrid all verified UNCHANGED). Boundary-TYPE change only → subgrid.nc byte-identical, reused.
- **CRITICAL container catch:** `sfincs-desktop.sif` = **v2.3.3 mt.Faber+** (the build that made snapwave_tuned); `sfincs-cpu.sif` = **v2.4.0 Galibier (2026-06-11) — ALREADY ON DISK** (this is Workstream F's target, no container rebuild needed for F!). Must run C on sfincs-desktop.sif or the wall A/B conflates with a version change. Submitted via `SFINCS_SIF=$PWD/sfincs-desktop.sif sbatch --time=03:00:00 hpc/sfincs_run.slurm experiments/nw_{open,wall}` — the SLURM script's 1h cap is too short (run ≈2h/7237s) and its default SIF is the WRONG (Galibier) one. Jobs 57932286 (nw_open) + 57932287 (nw_wall), RUNNING on main-redhat (preemptible — may need resubmit).
- Analysis ready: `scripts/analyze_c.py` — (1) gauge-peak A/B open-vs-wall, (2) tidal-range A/B, (3) estuary storage-VOLUME budget from map zs + subgrid z-table (wall RAISES vol⇒water was escaping; LOWERS/≈⇒boundary was a source/neutral, not a leak), (4) direct N/NW flux if storevel var names cooperate (needs inspection of the map output — velocity var names TBD). EXPECTED per forcing audit: small effect on estuary (bay Dirichlet-pinned to Battery 3.42=correct obs, so wall can only super-elevate above obs) → further localizes deficit to conveyance/damping.
- **2026-07-09 SESSION:** nw_open FINISHED CLEAN (73 tsteps, full 10-28→10-31; total_runtime ~2.1h, SnapWave 76%). **nw_wall (57932287) had FAILED** — SLURM cancelled at "5% complete" DUE TO TIME LIMIT (fell back to the script's 1h `-t` default / preemption; NOT slow — its own log estimated ~2.6h total, in line with nw_open). Deleted its truncated map/his, RESUBMITTED as **job 57962044** with `SFINCS_SIF=$PWD/sfincs-desktop.sif sbatch --time=03:00:00 hpc/sfincs_run.slurm experiments/nw_wall` — confirmed TIME_LIMIT 3:00:00, RUNNING on hal0352 (~2h, preemptible → resubmit same cmd if it dies). Inputs all intact/reused (subgrid byte-identical).
- **§3 DIRECT-FLUX CALC WIRED into `scripts/analyze_c.py` (validated on nw_open).** `storevel=1` → map has face-centred `u`,`v` (m/s); grid EPSG:32618 UNROTATED so `v`=true north. New helpers: `_cell_geometry()` (exact per-face width/ymin/ymax from 1-based `mesh2d_face_nodes`+node coords — quadtree cells axis-aligned) and `_boundary_flux()` = net northward vol flux across y=WALL_Y (4.48M) integrating v·H·width over the 733 active cells straddling the line. Also VECTORIZED `_cell_volume` (searchsorted, no Python loop) + switched §2/§3 zs reads to full-load-then-subset (scattered `.isel` on the 1.1GB compressed map TIMED OUT at 2min; full load = 4s). Whole script now runs in seconds. **KEY OPEN-RUN FINDING (refutes escape hypothesis): net flux across the N/NW line is SOUTHWARD/INTO the bay — peak −28,157 m³/s @ 10-29 20:00, time-integrated NET INFLOW 1.69×10⁹ m³. The boundary is a SOURCE feeding the bay, not a leak → walling removes real inflow, should LOWER estuary levels, confirming the boundary is not the under-fill culprit.** (matches forcing audit: Battery@3.42 Dirichlet feeds water in.)
- **REPORT UPDATED 07-09:** `reports/shrewsbury_investigation.md` now has a Workstream C section (general-audience prose) leading with the flux finding (boundary is a SOURCE, +1.7e9 m³ net inflow, escape refuted), wall A/B framed as pending.
- **WORKSTREAM D STARTED 2026-07-09 (wind sensitivity, current build, NO rebuild) — parallelized with C's wall run per user (bracket +20%/+30%).** `scripts/setup_d_wind.py` (mirrors setup_c_wall): copies baseline inputs, scales wind-drag `cdval` in the inp (stress = ρ_air·Cd·W² so Cd scale = stress scale; ONLY cdval changed, diff-verified) → `wind_cd120_25m` (×1.20 = 0.0012 0.003 0.003) + `wind_cd130_25m` (×1.30 = 0.0013 0.00325 0.00325). **RUN ON THE 25 m MESH, not the 12.5 m premier** (user's call): 25→12.5 m moved the Shrewsbury gauge only +0.04 m so resolution is non-binding → the wind DELTA is identical while each run is ~30% cheaper. **A/B baseline = the completed `snapwave_tuned_25m` (both arms share the 25 m mesh), NOT snapwave_tuned.** (First submitted from snapwave_tuned 12.5 m as jobs 57962176/77, then CANCELLED + re-staged at 25 m.) RUNNING jobs 57962196 (cd120_25m) + 57962197 (cd130_25m), 3h cap, sfincs-desktop.sif v2.3.3, on hal0354. Evaluate on basin-partitioned inner HWMs + river gauges + tidal range (NOT pooled HWM): does more wind lift bay→3.4 AND help rivers without overshooting validated seaward levels? **NOTE: C stays at 12.5 m (nw_open already done there — switching would waste it); default FUTURE sensitivity runs (D2 Holland, E IG) to 25 m.**
- **WORKSTREAM G (cleanup) PARTLY DONE 2026-07-09 (in parallel with runs):** G1 `git rm --cached sfincs-env.tar.gz` DONE (867 MB untracked, local kept, staged deletion `D` — user commits). G2 pruned `experiments/{igwaves,igwaves_wind,wind_waves}` (6.8 GB) — user-approved after verifying their metrics_25m.csv rows + `floodmaps/*_hmax_lev3.tif` are preserved. G3 audited `archive/models/` + deleted X1 dupes `{model_quadtree, notebooks_model_quadtree_x2}` (2.6 GB, user-approved); **KEPT `model_quadtree.good` (backup) + `model_quadtree_x2` (X2 experiment)** — offer these later. Repo 75G→67G. (NOTE: auto-mode classifier BLOCKS `rm -rf` of dirs not explicitly named in-session even if plan-approved — must get per-session naming; don't work around.)
- **ANALYZE_D PREPPED 2026-07-09:** `scripts/analyze_d.py` runs `V.evaluate()` on `snapwave_tuned_25m` (baseline) + both wind runs, tabulates the D yardsticks with deltas: `gauge_mod_peak_full_m` (bay→obs~3.4), `shrewsbury_mod_peak_m`/`_peak_err_m`, `hwm_bias_shrewsbury_navesink_m` (estuary), `hwm_bias_atlantic_oceanfront_m` (overshoot guard), tidal ranges. Thread-the-needle verdict logic built in. **CAVEAT being verified:** first smoke-test threw an xarray-2026.4.0 `timemax` decode error, BUT isolated repro showed `mod.output.read()`+`zsmax.max('timemax')` WORK fine (plain xr.open_dataset decodes too; cftime installed) → likely a caught per-fn error or transient; full baseline `evaluate()` re-run in progress to confirm evaluate produces a clean row. (User OK'd changing the sfincs env / installing pkgs if a real decode fix is needed.)
- **WORKSTREAM F DONE + E STARTED 2026-07-09 (Galibier upgrade + first IG re-test).** F1 dev-page confirmed (sfincs.readthedocs.io/en/latest/developments.html): v2.4.0 Galibier (2026.01, rel June 2026) — **"Fixed bug in SnapWave IG source term implementation"** + "Improvements of the integrated SnapWave solver for wave breaking" + **"Fixed bug with wavemakers, with waves forced from the north"** (⚠ BREAKING: "Renaming of wavemaker related input variables e.g. wavemaker_wvmfile" — matters if re-running wavemaker on Galibier) + new `huvmin`. Subgrid wet-fraction format UNCHANGED since v2.1.1 (Van Ormondt 2024) → our tables load, NO rebuild. `theta=1.0` default since v2.2.0 (so v2.3.3 already ran 1.0). **F2 NOT NEEDED: `sfincs-cpu.sif` verified = "v2.4.0 Galibier Release" (SLURM DEFAULT SIF — submit Galibier runs with NO SFINCS_SIF override).** **KEY: `wave_igwaves=True` only flips the `snapwave_igwaves` inp flag (model.py:272) — IG generated internally from the existing incident-wave boundary bhs/btp/bwd/bds, NO extra IG input file → an IG run = 1 flag on frozen-mesh inputs.** `scripts/setup_e_galibier.py` staged 2 runs from `snapwave_tuned_25m` (frozen 25 m, reused): `galibier_base_25m` (+theta=1.0 only → PURE version-regression vs snapwave_tuned_25m/v2.3.3) + `igwaves_galibier_25m` (+theta=1.0, snapwave_igwaves 0→1 → PURE IG-effect diff vs base). Diffs verified. SUBMITTED jobs 57962537 (base) + 57962538 (IG), 3h cap, DEFAULT Galibier SIF, PENDING behind the C/D runs. **CHECK on start: does Galibier LOAD the subgrid (F3) + does IG stay stable (v2.3.3 IG blew up bay Hm0 6.9e9)? — watch early log.** Next: when done, diff base-metrics vs snapwave_tuned_25m (must match ≈ → version clean); if IG stable, evaluate it + compare; the plan's E1 "decouple/deepen the SnapWave wave boundary" is the fuller follow-up if IG helps.
- **WORKSTREAM H STARTED 2026-07-09 (user's idea — widen the narrows to test if the eHydro ribbon is narrower than the real 2012 channel; the one residual B/bridge_dam flagged but never falsified).** DECISION (AskUserQuestion): AGGRESSIVE over-dredge, Galibier 25 m, A/B vs `galibier_base_25m`. Frame = falsification bracket like the C wall: if even an over-widened channel doesn't fill the estuary, WIDTH is dead as a cause → residual is distributed intertidal damping; if it DOES fill, eHydro under-captures real width (real fix). `scripts/make_narrows_dredge.py` → `data/elevation/narrows_wide_h.tif`: dilates the eHydro channel footprint +150 m each side, sets the ADDED banks to −5 m (leaves eHydro thalweg pixels NoData so survey depth still wins), clipped WEST of the Sea Bright barrier axis (x≤586000+0.075(y−4456000)) AND excludes VALID ground >+1.0 m (dune/causeway; NoData=water is includable) → 231,263 px = 5.78 km² added, 9,903 dune px excluded, NO barrier breach. `scripts/plot_narrows_dredge.py` → `reports/figures/narrows_widen_h.png` (visual check PASSED: clean ~150 m collar on both narrows, magenta stays west of barrier, threads around causeway). User APPROVED the dredge after 2 map iterations (added a `CAUSEWAY_CUTS` box x[586400,586900] y[4467850,4469400] to cut a wider slot THROUGH the Rumson–Sea Bright bridge causeway — the flagged choke — since GROUND_MAX was protecting it as "high ground"; left the Highlands/bay-mouth ground north of 4469400 protected = near barrier axis, opens to bay not a real choke). Final dredge: 235,168 px / 5.88 km² added, 3,905 causeway px cut through, 5,998 ocean-dune/upland protected, NO barrier breach. Map `reports/figures/narrows_widen_h.png`.
**BUILD IN PROGRESS 2026-07-09:** `narrows_wide_h` added to `data/data_catalog.yml` (RasterDataset, EPSG:32618). `experiments/narrows_wide_h` staged from snapwave_tuned_25m inputs. `scripts/rebuild_subgrid_h.py` RUNNING (bg task byus30aj8, ~20 min): loads frozen 25 m grid (547,267 faces, NOT rebuilt), reruns `sf.quadtree_subgrid.create(elevation_list=[narrows_wide_h]+DEFAULT, roughness_list=NLCD, nr_subgrid_pixels=8, nrmax=2000, write_dep_tif/man_tif)`, writes subgrid, sets theta=1.0, clears stale outputs. ("does not fully cover bbox" msgs = EXPECTED, local dredge + fallback layers.) **BUILD DONE + SUBMITTED 2026-07-09:** subgrid rebuilt (sfincs_subgrid.nc 178MB, dep/manning tifs, theta=1.0). VERIFIED dredge took effect: 8,774 narrows cells deepened (mean −3.24 m), Sea Bright choke mean z_zmin −0.58→−1.53 m, far-field max|Δz|=0.010 m (clean, local → A/B valid). SUBMITTED job **57963327** (`narrows_wide_h`), 3h cap, DEFAULT Galibier SIF, PENDING. **Evaluate vs `galibier_base_25m` on estuary HWM basin + Shrewsbury gauge + tidal range (reuse `scripts/analyze_d.py` pattern / V.evaluate): if estuary FILLS → width was the lever (eHydro under-captures real channel); if NOT → width dead, residual = distributed intertidal damping.** Build via `scripts/rebuild_subgrid_h.py` (reruns subgrid on frozen grid, reusable).
- **RESUME NEXT SESSION (2026-07-10) — START HERE. Session ended 07-09 EOD; 6 runs left going overnight, WATCHER STOPPED (no autonomous overnight analysis — user drives tomorrow).** (1) `squeue -u $USER` — **6 jobs submitted 07-09** (all 3h cap, PREEMPTIBLE main-redhat; if any DIED/missing, resubmit): C=**57962044** nw_wall (12.5m, ~2.5h slowest) [desktop.sif]; D=**57962196** wind_cd120_25m + **57962197** wind_cd130_25m [desktop.sif]; F=**57962537** galibier_base_25m + E=**57962538** igwaves_galibier_25m [Galibier/default sif]; H=**57963327** narrows_wide_h [Galibier/default sif]. RESUBMIT CMD: C/D → `SFINCS_SIF=$PWD/sfincs-desktop.sif sbatch --time=03:00:00 hpc/sfincs_run.slurm experiments/<name>`; E/F/H → `sbatch --time=03:00:00 hpc/sfincs_run.slurm experiments/<name>` (NO override = Galibier). **CHECK each done run's `sfincs_map.nc` has 73 tsteps. LOG "% complete" is BUFFERED/STALE — use `sfincs_his.nc` ntime (×600s, 72h total) for real progress.** (2) **C:** `NJ_ROOT=$PWD PYTHONPATH=$PWD micromamba/envs/sfincs/bin/python scripts/analyze_c.py` (needs nw_wall+nw_open); EXPECT wall LOWERS estuary (boundary is a SOURCE, +1.7e9 m³ inflow already shown) → escape refuted; write report §C verdict. (3) **D:** `…/python scripts/analyze_d.py` (evaluate confirmed clean on baseline; compares wind_cd120/130_25m vs snapwave_tuned_25m); add report §D. (4) **F/E:** diff `galibier_base_25m` metrics vs `snapwave_tuned_25m` (must ≈ match = version clean, F verdict); IG already stable in-progress (bay hm0 0.046 not 6.9e9) → evaluate `igwaves_galibier_25m`, check field hm0 max, compare to galibier_base_25m; write report §E/F. (5) **H:** evaluate `narrows_wide_h` vs `galibier_base_25m` (reuse analyze_d pattern) on estuary HWM + Shrewsbury gauge + tidal range → the width falsification verdict; write report §H. (6) Then G leftovers (offer archive/models .good + x2), plan's E1 (deepen SnapWave wave bnd) if IG helps.
- **RUN-INTEGRITY AUDIT 2026-07-10 (START HERE — supersedes the RESUME bullet's step 1). ALL 6 OVERNIGHT JOBS + snapwave_tuned_25m COMPLETED.** sacct all `COMPLETED` (nw_wall 57962044 2:42; wind 57962196/97 ~2h; galibier_base 57962537 1:24; igwaves 57962538 2:01; narrows_wide_h 57963327 1:11). Every run: **73 map tsteps / 433 his tsteps / t_end 259200 s (72 h)**, clean `----------- Closing off SFINCS -----------`, and **`Info: running SFINCS with subgrid bathymetry … number of subgrid levels : 10`** ⇒ **F3 ANSWERED: Galibier v2.4.0 loads our v2.1.1-format subgrid tables fine, NO rebuild.** Containers verified correct per-run (`Build-Revision` in log): C/D = `v2.3.3 mt. Faber+ / svn 197-branch:SnapWave_IG`; E/F/H = `v2.4.0 Galibier Release / git SFINCS_SnapWave:main`. Flags verified: cdval ×1.2 (0.0012 0.003 0.003) / ×1.3 (0.0013 0.00325 0.00325); `snapwave_igwaves=1` ONLY on igwaves_galibier_25m; theta=1.0 on the 3 Galibier runs. **Boundary forcing identical everywhere (max zs on msk==2 cells = 3.192 m in all 8 runs).** Diagnostic recipe: `zsmax` has dims `(timemax=3, nmesh2d_face)` → use `.max("timemax") - zb` for DEPTH (raw zsmax on dry land == bed elev, so "zsmax=72.66 m" is a real high-ground cell, NOT a blowup); his `station_name` is `|S256` → `s.decode().strip()`; 4 stations = `sandy_hook, usgs_stormtide_sea_bright, usgs_tidal_sea_bright, usgs_tidal_shark`. **`usgs_tidal_sea_bright` IS the Shrewsbury 01407600 gauge** (its nw_open peak 2.265 == validate.py's `shrewsbury_mod_peak_m`); obs crest = 2.935 m NAVD88.
- **⚠ GALIBIER TRANSIENT INSTABILITY (new, blocking F+H baseline).** All 3 v2.4.0 runs show **isolated single-snapshot spikes**; all 4 v2.3.3 runs are clean (domain max depth ~30 m). NOT divergence — max depth returns to ~27–28 m at the very next output step, model self-recovers; but **`zsmax` is a running max so one bad snapshot permanently poisons the zsmax field + any peak read from it.** Severity: `galibier_base_25m` max depth **2239 m**, hm0 **252 m** (spikes t=15,32,48 h); `narrows_wide_h` 1555 m / hm0 79.9 m (t=8,16 h); `igwaves_galibier_25m` mild, 73 m (one blip t=54 h). **Spikes are ALL in Raritan Bay / lower harbor, y>4,473,878 — NONE in the Shrewsbury/Navesink estuary** (narrows_wide_h: 0 estuary cells; galibier_base's "43 in estuary bbox" are a y<4.474M sliver at the bay mouth). **Estuary his gauges are spike-free in ALL 8 runs** (isolated-jump test, 0 flags) ⇒ science target uncontaminated. **BUT `galibier_base_25m` Sandy Hook is CORRUPTED**: his shows `1.16, 0.88, 3.34, 5.94, 5.01, 4.67, 1.88` around t=33 h (coincides with the t=32 h domain hm0→252 m), fake peak 5.940 vs true ~3.15; only 3/433 samples >3.6 m, p99=3.23 sane. **`huvmin` is NOT set in any inp** → Galibier uses its new default; prime suspect + cheap knob to test (rerun galibier_base_25m ≈1.4 h). **This matters because galibier_base_25m is the A/B baseline for BOTH F (version regression) and H (narrows widen).** Its estuary peak 2.479 is itself spike-free, but the t=33 h bay mass event may bias it → don't treat as final until re-run.
- **🔬 GALIBIER SPIKE ROOT-CAUSE NARROWED 2026-07-10 — it is the WAVE SOLVER, not thin-water advection. Two of my earlier claims were WRONG, corrected here.** (i) **`huvmin` is NOT new in Galibier**: the symbol is in the v2.3.3 Faber binary too; the v2.4.0 note reads "**Added input variable** 'huvmin'" i.e. newly EXPOSED as user input, not newly introduced. Docs (sfincs.readthedocs.io/en/latest/parameters.html, "advanced users" table): *"Minimum depth for calculating velocity (uv = q / max(hu, huvmin)), used for output and advection"*, **default 0.0** (= no floor, in BOTH versions) — so "Galibier changed the default and that broke it" is unsupported. (ii) **The IG source-term fix is v2.3.0 mt. Faber (2025.02), NOT v2.4.0** — so it was ALREADY in our v2.3.3 build where IG blew up to 6.9e9. The earlier F1 note crediting Galibier's IG fix is WRONG. Whatever made IG stable is something else in Galibier — most plausibly the v2.4.0 line *"Improvements of the integrated SnapWave solver for wave breaking, and resulting wave-induced setup, on **steeper coasts**."* **⚠ RETRACTED METRIC — do not reuse: "hm0 > γ·snapwavedepth violates the Baldock limiter" is NOT a bug detector.** Baldock is a *dissipation* formulation, not a hard cap (unlike Battjes-Janssen's Hrms≤γh), so hm0>γh is legal; the control proves it — the STABLE Faber run `snapwave_tuned_25m` racks up 44,990 such "violations" within 3 km of the Shrewsbury gauge. An earlier claim of "1,339 cells violate, max 283×" was meaningless. **USE AN ABSOLUTE THRESHOLD instead: Faber's domain-wide hm0 max is 11.1 m, so hm0 > 15 m ⇒ unphysical for Sandy.** Census (25 m mesh, cells×timesteps): `snapwave_tuned_25m` **0** (max 11.1 m); `igwaves_galibier_25m` **0** (max 9.6 m — Galibier can be spotless!); `galibier_base_25m` **155** cell-steps, ALL at the single snapshot **t=32 h**, 155 distinct cells clustered y 4473930..4475120 = the **Sandy Hook Bay MOUTH by the Sea Bright barrier (a steep coast)**, 5.09–5.27 km from the gauge, worst cell (587083,4474058) hm0 **252 m** in `snapwavedepth`=7.44 m; `narrows_wide_h` **13** cell-steps at t=8 h & 12 h, y up to 4485793 (Raritan Bay), ≥4.92 km from gauge. ⇒ **the blowup is a RARE, single-snapshot, spatially-clustered event, NOT a version-wide defect** — one Galibier run (igwaves) has a perfect wave field. Reads as stationary-solver **non-convergence** (`snapwave_niter=100` is "divided by 4 for the 4 internal sweeps" ⇒ ~25 iters/sweep). **⇒ `huvmin` is the WRONG FIX: it only floors uv=q/max(hu,huvmin) for output+advection, cannot touch hm0 ⇒ would muzzle the zs symptom while leaving the wave field garbage and silently biasing wave setup. DO NOT.** Deliberately NOT varied: `snapwave_sector` (our 360 is required by `snapwave_wind=1`; Deltares' ref nb's 180 comes with wind OFF ⇒ physics change, not a stability knob). Consider reporting to Deltares.
- **THE +0.26 m VERSION GAIN IS *NOT* SPURIOUS WAVE SETUP (checked 2026-07-10, user's hypothesis holds up).** Worry was that galibier_base's unphysical waves inflate the Shrewsbury gauge. Refuted three ways: (1) every hm0>15 m cell is **≥4.9 km from the gauge** (587052,4468841), at the bay mouth/Raritan Bay; (2) **within 3 km of the gauge the wave field is IDENTICAL across versions** — all-time max hm0 = 8.62 m and t=46–52 h max 8.37 m in Faber, Galibier-base, IG and narrows alike; (3) during the estuary peak window mean hm0 near the gauge is **LOWER** in galibier_base (0.682 m) than Faber (0.918 m) ⇒ *less* local wave setup yet a *higher* water level, i.e. the gain happens despite, not because of, wave forcing. Also the blowup is at t=32 h vs the estuary peak ~49 h. **Triangulation: `igwaves_galibier_25m` is a numerically CLEAN Galibier run (0 unphysical cells) and gives Shrewsbury 2.397 vs Faber 2.223 = +0.17 m; since IG slightly LOWERS levels (2.397 < base 2.479), version-alone ≈ +0.26 m is consistent.** Still needs the clean baseline to be publishable.
- **NEW PLAN 2026-07-10 (approved, plan mode) at `~/.claude/plans/crystalline-stargazing-candy.md`** — supersedes the 07-08 `alright-claude-time-to-deep-alpaca.md` (A–G done/executed). Reframes around the premier being UNSETTLED (convergence finding). Locked decisions: convergence investigation = THOROUGH (niter sweep + wave-setup isolation); sequencing = CONVERGENCE FIRST. Structure: CRITICAL PATH = Workstream I (I-1 niter sweep 100/200/400/800 asymptote test; I-2 combine niter+dtwave; I-3 isolate wave-setup waves-on/off at estuary; I-4 premier verdict OR "Galibier over-sets-up on steep coasts" finding + maybe Deltares report). UNBLOCKED-by-I: A peak re-tabulation (A1/A2; A3 done), H narrows differential verdict, E/IG evaluation. Then C/D optional confirms + G leftovers.
- **FABER-niter400 DISCRIMINATOR PRE-STAGED 2026-07-10 (NOT submitted — fire only if Galibier sweep keeps climbing = branch b).** `scripts/setup_faber_niter400.py` → `experiments/faber_niter400_25m`, staged from `snapwave_tuned_25m` with **ONLY** `snapwave_niter` 100→400 (no theta; Faber default 1.0) ⇒ pure niter comparison vs the Faber premier on the SAME Faber engine. **PURPOSE:** we've only swept niter on Galibier; Faber is pinned at 2.223 = Faber at the *unconverged* niter100. This tests: Faber-400 stays ~2.2 → niter-sensitivity is Galibier-SPECIFIC (its wave-breaking rework), Faber = stable premier candidate (Galibier reserved for IG); Faber-400 climbs toward ~2.9 → under-convergence is GENERAL to SnapWave, Faber-converged may match obs best. **⚠ SUBMIT ON THE FABER SIF (not default Galibier):** `SFINCS_SIF=$PWD/sfincs-desktop.sif sbatch --time=03:00:00 hpc/sfincs_run.slurm experiments/faber_niter400_25m`. Guard skips re-stage if map exists. Script staged for commit. (My take: likely outcome is Faber-for-surge/estuary + Galibier-for-IG two-engine split; and if branch-b, the Galibier estuary overshoot = wave setup in a SHELTERED back-bay = physically suspect = worth Deltares. A3 tidal over-damping survives whichever engine wins.)
- **I-1 SWEEP LAUNCHED 2026-07-10:** extended `scripts/setup_i_wavestab.py` (added niter200/800 arms + a **clobber-guard**: skips staging any arm whose sfincs_map.nc exists, so re-running won't wipe completed runs — delete run dir by hand to force re-stage). Staged + SUBMITTED `galibier_niter200_25m` (job 57990455, -t 3h) + `galibier_niter800_25m` (job 57990456, **-t 8h**, may ~2× wave cost), DEFAULT Galibier SIF, PENDING. Sweep points now: niter100=galibier_base_25m (CORRUPTED, treat as bad/known) → 200 (pending) → 400=galibier_niter400_25m (3.235) → 800 (pending); dtwave600=3.610. **On completion: check hm0>15m census + domain maxdepth + Shrewsbury `usgs_tidal_sea_bright` peak per arm; does it ASYMPTOTE? plot peak vs niter.** setup_i_wavestab.py staged for commit.
- **⚠ dtwave600 CROSS-CHECK FAILED TO CORROBORATE niter400 2026-07-10 — premier NOT settled.** `galibier_dtwave600_25m` COMPLETED (job 57979792, 3h35m, 73 tsteps). Gauges CLEAN (0 spikes; residual blowup 6 cells at t=4h & t=57h, both AWAY from surge peak t=49h, so peaks uncontaminated). BUT it DISAGREES with niter400: Shrewsbury **3.610** (niter400 3.235, Faber 2.223; obs 2.935) → +0.68 overshoot; Sandy Hook **3.452** (niter400 3.182). **Two convergence knobs give answers 0.38 m apart AND neither fully kills the blowup (niter400 3 cells, dtwave600 6) ⇒ NO knob-independent converged answer; niter400 is NOT confirmed as "the converged premier."** Estuary keeps RISING + overshooting as the solver is pushed harder (Faber 2.22 → niter400 3.24 → dtwave600 3.61) — the converged Galibier estuary level is ≥3.2 m, ABOVE the observed crest. **niter400 remains the BEST candidate** (cleanest; its only blowup is POST-peak t=64h, so its t=49h peak is the most trustworthy; dtwave600's t=4h blowup may inject early water inflating its level). NEXT to actually pin convergence: combine knobs (niter400 + dtwave600 together) and/or a proper niter sweep (100/200/400/800 → does estuary asymptote?), and/or investigate the Galibier wave-breaking-on-steep-coasts SETUP itself (may be systematically over-setting-up the estuary). The "finalize premier then re-tabulate A/C/D peaks" plan is now LONGER than hoped — premier convergence is its own mini-investigation.
- **REPORT C & D SECTIONS UPDATED 2026-07-10 (STAGED, user commits).** C: replaced "wall pending" with the completed wall A/B (open→wall Sandy Hook 3.15→4.97 = +1.8m super-elevation vs obs ~3.4, Shrewsbury 2.27→2.87 = +0.6m → wall is a DIAGNOSTIC not a fix, confirms boundary not the cause) + audit note (C runs clean Faber-niter100, flux finding barotropic/premier-independent → STANDS; reframe from "explains under-fill" to "boundary is a source not a leak"). Added new D section (wind ×1.20/×1.30 → Shrewsbury +0.002/+0.004m = NULL lever; audit note: clean Faber runs, differential-null robust, soft spot = wind feeds waves so optionally re-confirm on converged engine; reframe off under-fill premise). Header "Covered here" → A,B,C,D. **REPORT F SECTION FIXED 2026-07-10 (STAGED):** corrected the IG-source-term-bug attribution (it's v2.3.0 Faber, already in our premier — added a dated Correction callout; the real Galibier change = wave-breaking-on-steep-coasts rework, plausible cause of BOTH IG stability AND the I blowup); added a "not a free upgrade" Caution (steep-coast spikes, disagreeing fixes, premier unsettled, A peaks blocked; IG stability unaffected = clean run); refreshed the stale "Still to come" (C/D were "finishing/running" → now lead with settle-converged-premier as critical path, + H/E). **narrows_wide_niter400_25m COMPLETED 2026-07-10 (job 57986441, 1h28m) — the clean width A/B (dredged subgrid vs galibier_niter400 baseline, both niter400); READY to analyze (differential read, premier not absolute).**
- **C & D AUDITED for niter100 exposure 2026-07-10 — both CLEAN, much safer than A.** All C/D runs are **Faber niter100** (nw_open/nw_wall 12.5m/672103 faces; wind_cd120/130_25m 25m/547267 faces) — NOT Galibier, so no wave-breaking-on-steep-coasts change → **hm0>15m census = 0 cellsteps in ALL four**, domain max depth ~30m, max hm0 11–13m. The blowup is Galibier-specific; Faber-niter100 stayed converged (no spurious energy). Exposure analysis: (1) **NO blowup corruption** (unlike galibier_base). (2) **Convergence exposure LOW because both are A/B differentials** sharing common-mode Faber-niter100: C's wall is a BAROTROPIC boundary change orthogonal to waves (robust); **C's flux headline (boundary=SOURCE, +1.7e9 m³ inflow, escape refuted) is ~99% barotropic/wave-independent → STANDS regardless of premier**; D's wind couples to waves via snapwave_wind=1 (only second-order niter path) but the result is a NULL (+0.002/0.004m) and under-convergence adds noise not signal → direction robust. (3) **SAME MOTIVATION-REFRAME AS A: C & D were motivated by the under-fill; if the converged premier overshoots (+0.30) not under-fills, their DIAGNOSTIC framing partly dissolves** — C's "how much super-elevation to fill the under-filled rivers" and D's "does wind fill them" both lose their premise. BUT their CORE characterizations (C: N boundary is a source not a leak; D: wind magnitude is a null lever) are premier-independent and survive — reword the writeups away from "explaining the under-fill," like A's tidal-range survived but its peak-deficit framing didn't. ACTIONS: C flux = no re-run; C wall + D wind = reframe + (optional, cheap) confirm on converged premier (D especially since wind touches waves); fold C/D baselines into the "finalize converged premier then re-tabulate" plan. Nothing on fire.
- **REPORT `reports/shrewsbury_investigation.md` A & B SECTIONS UPDATED 2026-07-10 for the convergence finding (STAGED, user commits).** Added: a top-of-file revision note; rewrote the short-version synthesis (tide over-damped = robust; storm-peak deficit = under-convergence artifact that reverses to +0.30 on converged run); B verdict callout (B immune — subgrid byte-identical across all runs, needs no re-run — AND B now agrees with A: faithful channel ⇒ surge peak gets in once waves converge; plus the "B verified the SURVEY not reality → H tests width" boundary); A section callout splitting the 3 yardsticks (§3 tidal robust/survives = new A headline; §1 gauge crest & §2 HWM basin = peak artifacts, methods valid but values re-tabulate on final premier); revised "Putting it together" table (original vs converged-solver reading) + next-steps ("lead with tidal range", finalize converged premier before re-tabulating peaks). Original 07-08 numbers preserved for the record with revised readings flagged. Staged alongside scripts/setup_{i_wavestab,h2_narrows_niter400}.py + the corrected setup_e_galibier docstring.
- **🔁 WORKSTREAM A RE-OPENED 2026-07-10 by the I convergence finding — its VERDICT is premier-dependent; the CODE is fine.** A was "DONE 07-08" on `snapwave_tuned` (Faber, niter100). The validate.py FRAMEWORK (SHREWSBURY_CREST datum, `classify_hwm_basin`, `tidal_range_metric`) is version-independent and stays done. But A's headline — "estuary UNDER-fills, 3 yardsticks agree (gauge −0.67, HWM −0.27, tidal range −0.53)" — was computed on a premier we now know under-converges (niter100). Direct check 2026-07-10 (gauge `usgs_tidal_sea_bright`, obs crest 2.935 m; pre-storm tidal range on 2,671 wet channel cells d<1.5km/bed<−1m, obs 1.37 m): **Faber niter100 peak 2.223 (deficit −0.71) | Galibier niter400 CONVERGED peak 3.235 (OVERSHOOT +0.30) | Galibier IG niter100 2.397 (−0.54).** **⇒ the STORM-PEAK yardsticks (A1 gauge crest, A2 HWM basin — both peak quantities) FLIP toward match/overshoot on the converged solver; A's "under-fill" is partly a Faber under-convergence ARTIFACT.** **BUT the pre-storm TIDAL RANGE (A3) is ROBUST/version-independent: ~1.00 m in ALL three runs (Faber, niter400, IG), err −0.37..−0.38 vs obs 1.37** — because it's the first-24h window before the t=32h blowup and pre-storm waves are tiny, so niter doesn't touch it. (My 1.00 differs from validate.py's official 0.84 = different cell-selection/median method + hourly-map under-sampling; the INVARIANCE across runs is the point, not the absolute.) **NEW A NARRATIVE: the conveyance deficit lives in the ASTRONOMICAL TIDE propagation (over-damped ~0.4 m, robust) NOT the storm-surge peak (gets in fine once waves converge). Materially different from the 07-05/07-08 "under-fills at peak" story.** TO CLOSE A: (1) pick the premier (pending dtwave600 convergence x-check + narrows_wide_niter400 re-baseline), (2) re-run A1/A2 peak yardsticks on it via `V.evaluate()`; A3 needs NO re-run. **PRACTICAL: `V.evaluate(dir)` full-loads the model and TIMES OUT >2min per run — for quick A-yardstick checks read his/map directly (gauge peak from his `usgs_tidal_sea_bright`; tidal range from map first-24h wet channel cells), don't call evaluate() in a loop.**
- **🚨 WORKSTREAM I RESULTS 2026-07-10 — OVERTURNS the "+0.26 m version gain" AND invalidates galibier_base_25m as a baseline. Two of 3 arms done (dtwave600 still running, 85%).** (1) **CONTROL `galibier_repeat_25m` is BIT-IDENTICAL to galibier_base_25m** — 155 hm0>15m cell-steps, hm0 252.2 m, depth 2238 m, spike at t=32 h, Sandy Hook 5.940, Shrewsbury 2.479, all to 3 decimals. ⇒ **the blowup is DETERMINISTIC (not OpenMP reduction-order), so the knob arms are interpretable.** (2) **`galibier_niter400_25m` (snapwave_niter 100→400) nearly cures it**: 155→**3** cell-steps (residual at t=64 h, AFTER the surge peak so harmless), and BOTH gauges go CLEAN — Sandy Hook **3.182** @49 h smooth 0-spikes (was fake 5.940 spike), Shrewsbury **3.235** @49 h smooth 0-spikes. (3) **CORRUPTION OF BASE IS REAL & THROUGHOUT, not just the one snapshot:** in base the t=33 h blowup CRASHES the state — Sandy Hook drops to 0.63 m at t=34 h while niter400/Faber both rise to 1.14 m; base's Shrewsbury "peak" 2.479 is actually AT t=33.3 h (the blowup), and at the REAL surge peak t=49 h base Shrewsbury is only ~1.6–2.1 m vs niter400's 3.2 m. **⇒ galibier_base_25m (and galibier_repeat) are UNUSABLE baselines — the earlier "+0.26 m version gain (base 2.479 vs Faber 2.223)" is RETRACTED: 2.479 was a blowup transient at the wrong time, not a surge peak.** (4) **niter is NOT just a stability knob — it changes the ANSWER** (niter100 was under-converged, sometimes catastrophically): so all niter100 IG-off results are suspect. Spatial cross-check at t=49 h (estuary box, wet cells>0.05 m): Faber 44,789 cells/mean_zs 3.95 m < base 45,897/4.04 < **niter400 51,953/4.45** — niter400 fills most; the single Shrewsbury gauge cell (bank, bed 1.455) exaggerates the spread (shows ~1 m) vs spatial mean (~0.4 m), so lean on spatial/multi-cell, not that one cell. (5) **CLEAN Galibier runs so far: niter400 (IG off, Shrewsbury 3.235) + igwaves_galibier_25m (IG on, niter100, 0 blowup cells, Shrewsbury 2.397).** No clean niter100 IG-off Galibier run exists (base is corrupt). (6) **⚠ CONSEQUENCE: the H narrows verdict is now SUSPECT** — narrows_wide_h (Galibier, niter100, IG off, had 13 blowup cells) was compared vs the corrupted galibier_base ⇒ its "+0.81 m, width is a lever" headline must be RE-RUN with niter400 on both arms before trusting. Likewise re-establish the version delta with a converged solver. **OPEN: does niter400 overshoot obs? Shrewsbury 3.235 vs crest 2.935 = +0.30 m high (but Sandy Hook 3.182 ≈ obs 3.42, honest). Need: (a) confirm 400 is converged not just 'more' (dtwave600 arm is an independent cross-check — different mechanism, still running); (b) a clean niter400 IG-off Galibier baseline is now THE baseline — rebuild narrows_wide_h + wind + IG comparisons against it.** VERDICT DIRECTION: the Galibier blowup was SnapWave stationary-solver NON-CONVERGENCE at niter100 (as hypothesized); niter400 fixes stability AND raises the estuary — so the version-upgrade story is real but its MAGNITUDE is unknown until re-baselined. Report to Deltares still warranted (niter100 is the shipped default yet under-converges to 252 m waves).
- **🚨🚨 I-1 SWEEP COMPLETE 2026-07-10 — niter does NOT converge; it is NON-MONOTONIC/bistable ⇒ NO converged premier from this knob ⇒ branch-(b) confirmed direction (Galibier stationary SnapWave solver is unstable on this steep coast, not merely under-converged).** Both overnight arms finished (niter200 job 57990455 COMPLETED 1:24; niter800 job 57990456 COMPLETED 1:42; inp `snapwave_niter` verified 200/800; his md5s all distinct = genuinely different runs). **RESULT: niter100 (galibier_base), niter200, AND niter800 all collapse into the SAME blown-up regime — Sandy Hook GLOBAL max 5.940, Shrewsbury global max 2.479, to 3 decimals identical; global peak is at an EARLY TIDAL step (2012-10-29T09:20, idx 200), i.e. their storm-surge window peak is LOWER than the pre-storm tide.** Only **niter400 escapes** to a clean island: Sandy Hook 3.182 @surge, Shrewsbury 3.235 @2012-10-30T01:00 (true surge peak). Surge-window Shrewsbury: niter200 **2.320**, niter400 **3.235**, niter800 **2.323** (200≈800 in blowup regime ≈2.32; 400 outlier 3.24). **So the sequence 100→200→400→800 is blowup / blowup / CLEAN / blowup — does NOT asymptote; niter400's 3.235 is an isolated clean point, not a converged limit.** This + the earlier dtwave600=3.610 disagreement ⇒ the two convergence knobs never agree AND raising niter doesn't monotonically settle ⇒ **the SnapWave stationary solver is not converging on Galibier for this domain; there is no knob-independent Galibier premier.** ⇒ **LAUNCHED the branch-(b) FABER-niter400 DISCRIMINATOR: job 57995406** (`experiments/faber_niter400_25m`, ONLY snapwave_niter 100→400, Faber sif sfincs-desktop.sif, -t 3h, PENDING on main-redhat/preemptible → resubmit `SFINCS_SIF=$PWD/sfincs-desktop.sif sbatch --time=03:00:00 hpc/sfincs_run.slurm experiments/faber_niter400_25m` if it dies). **DECISION LOGIC on its return:** Faber-400 stays ~2.2 ⇒ niter-sensitivity is Galibier-SPECIFIC (its wave-breaking-on-steep-coasts rework) ⇒ Faber = stable premier for surge/estuary, Galibier reserved for IG; Faber-400 climbs toward ~2.9 ⇒ under-convergence is GENERAL to SnapWave ⇒ Faber-converged may match obs best. Either way: **Galibier niter100 (shipped default) under-convergence to 5.94 m Sandy Hook / 252 m waves = worth a Deltares report.** REMAINING I: I-2 combine niter+dtwave (deprioritized — knobs already shown to disagree), I-3 waves-on/off at estuary, I-4 premier verdict pending Faber-400.
- **✅ FABER-niter400 DISCRIMINATOR RETURNED 2026-07-10/11 — DECISIVE: niter is INERT on Faber ⇒ Faber is the STABLE PREMIER; the Galibier estuary lift is a version-specific solver artifact, NOT a truer fill.** Job 57995406 COMPLETED clean (1h38m, 73 tsteps, `Closing off SFINCS`, SnapWave converged @iter13). Field CLEAN: **hm0>15m census = 0 cell-steps, domain hm0 max 11.08 m** (physical), zsmax.max−zb sane. Gauge peaks: Sandy Hook **3.154**, Sea Bright storm **3.643**, **Shrewsbury `usgs_tidal_sea_bright` 2.223**, Shark R 1.260. **THE RESULT: Faber-niter400 Shrewsbury 2.223 == Faber-niter100 2.223 to 3 decimals — raising niter 4× moved the estuary peak 0.000 m.** ⇒ lands on the pre-declared branch "Faber-400 stays ~2.2 ⇒ niter-sensitivity is GALIBIER-SPECIFIC (its wave-breaking-on-steep-coasts rework), Faber = stable converged premier." **VERDICTS: (1) Faber's estuary under-fill is NOT under-convergence — Faber is already converged at the shipped default niter100, the knob is inert. (2) Galibier is NOT a trustworthy premier — non-monotonic/bistable across niter (only accidentally clean at 400; 100/200/800 all collapse to the 5.94/252m blowup), so its higher estuary fill 3.235 comes from an unstable over-setup regime, not a truer solution. (3) ⇒ FABER = PREMIER; the "premier is UNSETTLED" 07-10 shift is now SETTLED. (4) The estuary under-fill is REAL and version-independent: Faber 2.223 vs obs crest 2.935 = −0.71 m, re-confirming the 07-05 [[project_bridge_dam]] distributed-conveyance-ceiling verdict and the three-yardstick agreement (gauge −0.67 / HWM basin −0.27 / tidal range −0.53). (5) The ~1 m Faber↔Galibier gap at matched niter400 is a genuine Galibier physics change (steep-coast wave-setup rework over-sets-up the sheltered back-bay — physically suspect), NOT a convergence artifact both should agree on ⇒ Deltares report still warranted (niter100 shipped default under-converges to 252 m waves on this steep coast).** **CONSEQUENCE for the reopened peak yardsticks (A1/A2): the "+0.30 overshoot on the converged solver" reading is RETRACTED — that was Galibier's artifact; on the real premier (Faber) the storm-peak under-fill STANDS at −0.71 m, so A1 gauge-crest & A2 HWM-basin deficits are REINSTATED, not flipped. A3 tidal-range robust as always. narrows_wide_niter400 H verdict must be read as a Galibier-baseline differential only — NOT transferable to the Faber premier's absolute level.** NEXT: re-tabulate A1/A2 on `snapwave_tuned_25m` (=Faber premier) via direct his/map reads (NOT evaluate() in a loop); update report I/A/premier sections; consider whether IG (Galibier-only, stable, but −0.08 m = doesn't help) is worth a two-engine split at all given Faber is premier.
- **🔎 GALIBIER BLOWUP ROOT-CAUSE FOUND IN SOURCE 2026-07-11 (code-level, decisive). It is a REMOVED WAVE-HEIGHT CLAMP, not a mystery.** SFINCS is open-source (github.com/Deltares/SFINCS; SnapWave solver = `source/src/snapwave/snapwave_solver.f90`, defaults in `source/src/sfincs_snapwave.f90`). The **Galibier release commit `1f1d8286` "2026.01 galibier release"** changed two COUPLED SnapWave defaults: **`snapwave_gammax` 2.0→`999.0`** (confirmed literal in main's sfincs_snapwave.f90: `read_real_input(...,'snapwave_gammax',gammax,999.0)`) and **`snapwave_baldock_exponent` 0→`2`** (steep-coast breaking dissipation; also new `snapwave_relax_factor_DoverE/DoverA=0.25` under-relaxation). Commit msg: *"Change default snapwave_gammax to 999 now, which works because of snapwave_baldock_exponent=2"* and *"baldock_exponent=2 … only active for steep coastlines, where Baldock dissipation can be too low in the surf zone."* **MECHANISM:** solver has TWO wave-height limiters — (1) `gammax` = a BLUNT HARD CLAMP applied EVERY sweep inside the stationary iteration (`depthlimfac=max(1,(sqrt(Ek/rhog8)/(gammax*depth))**2); ee=ee/depthlimfac` ⇒ forces Hrms≤gammax·depth), and (2) Baldock breaking = smooth physical dissipation ramping as H→gamma·depth. **Faber (gammax≈2): the hard clamp is ALWAYS live ⇒ unconditionally stable even when the iteration under-converges (crude, but bounds runaway energy every sweep — and likely why Faber under-sets-up/under-fills). Galibier (gammax=999): clamp effectively DISABLED ⇒ wave height controlled ONLY by Baldock dissipation, which is a nonlinear in-iteration feedback (Dw↔H) that is STIFF on steep coasts (sharp depth gradients = the Sea Bright bay mouth) and FAILS to converge at niter100 with no backstop ⇒ energy runs away to Hrms 252 m.** This explains EVERY symptom: Galibier-specific (Faber has the clamp); only at the steep bay mouth not the estuary (Baldock-exponent targets steep slopes); non-monotonic niter (100/200/800 blow up, 400 clean = stiff nonlinear iteration near a bifurcation with no clamp); and the ~1 m estuary lift = removing the clamp lets real setup through BUT it rides on an unclamped non-converged solve ⇒ magnitude untrustworthy. **⇒ CONFIRMS Faber=premier mechanistically. ACTIONABLE DISCRIMINATOR (cheap, 1-line inp, no rebuild): run Galibier with `snapwave_gammax=2` (restore Faber's clamp) OR `snapwave_baldock_exponent=0` (restore Faber's dissipation) off galibier_niter400_25m / a niter100 Galibier — isolates "IG-bugfix/steep-coast physics genuinely helped" vs "removed clamp let an unconverged solve run hot": lift survives clean ⇒ real; collapses to ~2.2 ⇒ was the missing clamp. Gives a STABLE Galibier that keeps working IG either way.** Also grounds the Deltares report: shipped default gammax=999 + niter100 under-converges to 252 m waves on steep coasts (the Roelvink-et-al-2025 GMD-consistent solver rework, v2.4.0).
- **CLAMP-vs-DISSIPATION DISCRIMINATORS SUBMITTED 2026-07-11 (`scripts/setup_clamp_experiments.py`, staged from `snapwave_tuned_25m`, frozen mesh, one-knob-each verified by inp diff).** **RUN 1 `galibier_gammax2_25m` (job 58036930, DEFAULT Galibier sif, theta=1.0+niter400+`snapwave_gammax=2.0`)** = the clean `galibier_niter400_25m` (3.235) with ONLY gammax 999→2 (restore Faber's clamp on a converged Galibier solve) — THE MONEY RUN. **VERDICT LOGIC: Shrewsbury `usgs_tidal_sea_bright` drops toward ~2.2 ⇒ the ~1 m lift WAS the removed clamp (Faber under-fills *because* of its clamp), Faber-premier fully vindicated; stays ~3.2 with clean hm0 (<15 m) ⇒ lift is REAL Baldock steep-coast physics and `galibier_gammax2` is a stable improved-premier candidate.** **RUN 2 `faber_bexp2_25m` (job 58036931, FABER sfincs-desktop.sif, stock Faber premier + `snapwave_baldock_exponent=2`)** = does Galibier's surf-zone dissipation move Faber WITH its clamp still on (expect small/null ⇒ confirms lift=clamp not dissipation). **⚠ VERIFY Faber honored the keyword: diff its hm0 field vs snapwave_tuned_25m — byte-identical ⇒ Faber v2.3.3 binary silently ignored `snapwave_baldock_exponent` (added-in-Galibier keyword) ⇒ run==stock Faber, INCONCLUSIVE (not a null).** Both jobs 3h cap, main-redhat PREEMPTIBLE (resubmit: RUN1 `sbatch --time=03:00:00 hpc/sfincs_run.slurm experiments/galibier_gammax2_25m`; RUN2 `SFINCS_SIF=$PWD/sfincs-desktop.sif sbatch …/faber_bexp2_25m`). On completion: hm0>15m census + domain maxdepth + Shrewsbury/Sandy Hook his peaks per run. **RUN 3 BUILT + SUBMITTED 2026-07-11 (user approved) `faber_gammax999_25m` (job 58037184, FABER sfincs-desktop.sif, stock Faber premier + `snapwave_gammax=999.0`, one knob = clamp OFF on the stable Faber engine): Faber-no-clamp ALSO explodes ⇒ clamp is the whole story engine-independent; stays stable ⇒ Galibier's solver rework added extra instability. A/B vs snapwave_tuned_25m (2.223). Completes the 2×2 with RUN 1.** **⚡ RESUBMITTED ALL 3 AT 48 CORES 2026-07-11: cancelled the first -c24 submissions (58036930/31 — they'd STARTED and left truncated partial maps → wiped dirs + re-staged pristine), bumped `hpc/sfincs_run.slurm` `-c 24→48` + `--mem 24→48G` + added guarded `numactl --interleave=all` (SnapWave scales ~1.6× 16→48 on these dual-socket nodes, runs are 85% SnapWave; Saturday = 238 idle nodes). Deterministic solver (galibier_repeat was bit-identical) so more cores don't change results. Jobs: RUN1 `galibier_gammax2_25m` 58037182 (Galibier sif), RUN2 `faber_bexp2_25m` 58037183 (Faber sif), RUN3 `faber_gammax999_25m` 58037184 (Faber sif); all -t 3h. slurm change + setup_clamp_experiments.py staged for commit.**
- **📖 PARAMETER PHYSICS pinned from source + XBeach docs 2026-07-11 (`snapwave_gammax` / `snapwave_baldock_exponent` — the "why 2 vs 999" answer).** **`gammax` and `gamma` are DIFFERENT things.** `snapwave_gamma`=0.78 = the PHYSICAL breaker index: Hmax=gamma·depth is the depth-limited height where Baldock breaking dissipation ramps (this is the physics; our inp sets 0.78). **`gammax` = a SEPARATE NUMERICAL SAFETY CAP**, hard-applied in the solver as `depthlimfac=max(1,(Hrms/(gammax·depth))²); ee=ee/depthlimfac` ⇒ forces Hrms≤gammax·depth. XBeach docs (publicwiki.deltares.nl XBeach Default settings): gammax = "maximum ratio wave height to water depth," **default 2.0, range 0.4–5.0**, "reducing it reduces wave heights in very shallow water." **So 2 vs 999 are NOT rival tunings of one physical knob — they're the SAME limiter ENGAGED (2) vs OFF (999).** 2·depth is already a very LOOSE cap (real breakers are H/h≲0.78, so a cap at 2·h almost never touches realistic waves — it only catches NUMERICAL runaway; hence 2 = conventional "on but loose"). 999·depth = effectively ∞ = limiter disabled. **Why 999 isn't reckless (Deltares' logic):** the `baldock` subroutine's own code comment says exp>0 "adds extra dissipation when Hloc>Hmax … needed at very steep coast lines, where Baldock dissipation cannot always keep up with the wave height increase due to shoaling" via `f=(Hloc/Hmax)^iexp`. With `baldock_exponent=2` the amplified dissipation is meant to hold H near 0.78·h by PHYSICS, making the gammax cap redundant → they park it at 999 to get the crutch out of the physics' way. **Design = ONE mechanism load-bearing at a time: Faber {crutch on gammax=2, standard dissipation exp=0} vs Galibier {dissipation load-bearing exp=2, crutch off gammax=999}.** THE SHARP EDGE (our bug): the amplified dissipation only holds H down IF the stationary solver CONVERGES; on our steep bay mouth at niter100 it doesn't — the exact "steep coast where Baldock can't keep up with shoaling" case the comment names — and with gammax=999 there's no backstop ⇒ 252 m. **REFRAMES RUN 1: `gammax=2` on Galibier is NOT a weird hybrid — it's the belt-and-suspenders config (Galibier's better dissipation WITH the loose safety cap re-engaged), arguably the most sensible setting of all. FOLLOW-UP LEVER: gammax range is 0.4–5, so if gammax=2 clips genuine setup, try gammax=5 (looser finite cap) to find where the net stops interfering with real physics.** Other params: Baldock coeff 0.28 (was 0.25 pre-2024, commit d4dd7b40); `baldock_ratio=0.2` = only compute breaking when Hk>0.2·Hmax (efficiency threshold); the whole Galibier solver = consistent with Roelvink et al. 2025 GMD ("SnapWave: fast, implicit wave transformation," gmd.copernicus.org/articles/18/9469/2025/).
- **WORKSTREAM I SUBMITTED 2026-07-10 — `scripts/setup_i_wavestab.py` (mirrors setup_e), 3 arms staged from `snapwave_tuned_25m` + theta=1.0, DEFAULT Galibier SIF, inp diffs verified = exactly one knob each.** **`galibier_repeat_25m` (job 57979790, -t 3h) = THE CONTROL, byte-identical inp to galibier_base_25m** — does the t=32 h spike reproduce? If YES the blowup is deterministic and the knob arms are interpretable; if NO the run is non-reproducible (OpenMP reduction order / thread count) and a "clean" knob arm proves NOTHING alone. **Do not interpret the knobs without this.** `galibier_niter400_25m` (job 57979791, **-t 8h**, snapwave_niter 100→400) tests non-convergence. `galibier_dtwave600_25m` (job 57979792, **-t 6h**, dtwave 1800→600 s) couples SnapWave 3× more often. **Walltimes sized deliberately: SnapWave was 72% of galibier_base's 1h24m, so niter400 ≈ 4× wave cost (~4.5 h) and dtwave600 ≈ 3× (~3.5 h) — the stock `hpc/sfincs_run.slurm` `-t 01:00:00` default would kill both (it already killed nw_wall once).** Verdict metric = the hm0>15 m census above + domain max depth (healthy ≈30 m) + Shrewsbury/Sandy Hook his peaks.
- **Docstring of `scripts/setup_e_galibier.py` CORRECTED 2026-07-10** (it asserted Galibier fixed the IG source term; that fix is v2.3.0 Faber). Staged, not committed.
- **Reference nb `notebooks/reference/build_quadtree_from_script_TKI-share_..._withigwaves_withwavemaker.ipynb` (Deltares TKI, run on the v2.3.0 Faber exe) — AUDITED 2026-07-10, NOTHING MISSING.** Its cell 46 SnapWave block = `snapwave 1, snapwave_igwaves 1, snapwave_wind 0, storefw 1, storewavdir 1, dtwave 1800, snapwave_alpha 1.0, snapwave_gamma 0.78, snapwave_hmin 0.01, snapwave_dtheta 5, snapwave_fw 0.02, snapwave_sector 180, snapwave_niter 100`. **We already set all 8 physics knobs at IDENTICAL values** (alpha 1.0, gamma 0.78, hmin 0.01, dtheta 5, fw 0.02, niter 100, dtwave 1800). Only intentional deltas: `snapwave_sector` 360 (vs 180) and `snapwave_wind` 1 (vs 0) — both required by our wind-growth setup, neither a stability knob. **Incidental find: the nb leaves `huthresh` at the SFINCS default 0.05 m; we set `huthresh=0.01` (5× more permissive wetting).** With `huvmin=0.0` + advection on, uv=q/max(hu,0) at hu≈0.01 m gives 5× more advective-velocity headroom than stock. Not the cause of the hm0 blowup (above), but worth knowing; `huvmin≈0.05` would floor the velocity WITHOUT changing wet/dry extent if a thin-water problem ever does appear.
- **PRELIMINARY HEADLINE NUMBERS 2026-07-10 (Shrewsbury gauge `usgs_tidal_sea_bright` peak, obs crest 2.935 m; his 10-min, NOT the full analyze_c/d scripts yet).** `snapwave_tuned_25m` (Faber base) **2.223** (−0.71); `galibier_base_25m` (Galibier base) **2.479** (−0.46) ⇒ **F: version alone is +0.26 m, NOT neutral** (only inp diff is theta=1.0, a documented no-op since v2.2.0 ⇒ genuine solver change); `narrows_wide_h` **3.287** ⇒ **+0.81 m over its Galibier baseline and a +0.35 m OVERSHOOT of the observed crest** ⇒ **H: WIDTH IS A LEVER — the aggressive over-dredge more than fills the estuary, so eHydro's ribbon plausibly under-captures the real 2012 channel width. This CONTRADICTS the 07-05 "distributed damping / structural ceiling, not a fixable knob" verdict and is the biggest result of the campaign.** Causal cleanliness checks PASSED: narrows_wide_h Sandy Hook 3.140 ≈ snapwave 3.154 (bay NOT elevated, so the rise is not injected mass) and the gauge cell's `point_zb` is **1.455 m in narrows_wide_h AND in both baselines** (dredge left that bank cell untouched — GROUND_MAX protected it) ⇒ rise is hydrodynamic, not geometric. `nw_wall` **2.868** (+0.60 vs nw_open 2.265) but bay super-elevated to Sandy Hook **4.967** (smooth/sustained, 58 samples >3.6 m, numerically CLEAN, max depth 30 m — this is a real response, not a spike) vs obs 3.42 ⇒ **C: +1.8 m of UNPHYSICAL bay super-elevation buys only +0.60 m in the river — larger than the "expect small" prediction, still the conveyance-deficit reading.** `wind_cd120_25m` 2.225 / `wind_cd130_25m` 2.227 vs base 2.223 ⇒ **D: NULL (+0.002/+0.004 m), wind drag is not the lever — done, write it up.** `igwaves_galibier_25m` 2.397 vs galibier_base 2.479 ⇒ **E: IG does not help (−0.08 m)**, BUT **IG IS NOW STABLE: `hm0ig` max 2.7 m (was 6.9e9 on v2.3.3) ⇒ the Galibier IG bugfix is real and the old "IG blows up" verdict was a bug artifact.** NOTE C runs are 12.5 m (gauge `point_zb` 1.383) vs D/E/F/H 25 m (`point_zb` 1.455) → never cross-compare across mesh families.
- **NEXT (2026-07-10):** (1) **Re-run `galibier_base_25m` with `huvmin` set explicitly** (and consider it for narrows_wide_h) to kill the Raritan Bay spikes → then redo the F regression + H A/B on a clean baseline; H's +0.81 m is very likely to survive (its estuary gauge + bay are already clean) but the baseline must be trustworthy. (2) Run `scripts/analyze_c.py` (nw_open+nw_wall both present & clean) and `scripts/analyze_d.py` for the full basin-HWM/tidal-range yardsticks — the numbers above are gauge-peak only. (3) H follow-up if it survives: the over-dredge overshoots, so bracket it — a *moderate* widening should bracket the true channel width; that turns H from a falsification test into a calibration. (4) Report §C/§D/§E/§F/§H.
- **GIT STATE (2026-07-10 handoff — user commits + pushes themselves).** Last session's A/B/C-prep work was COMMITTED as `b028a03` "Add Shrewsbury River investigation report and analysis scripts" (validate.py, probe_subgrid_conveyance.py, setup_c_wall.py, analyze_c.py v1, report v1). **THIS session (07-09) STAGED for user to commit** (Claude staged, per [[feedback_git_workflow]]): modified `reports/shrewsbury_investigation.md` (+§C wall-flux, +§F Galibier) & `scripts/analyze_c.py` (+§3 flux calc); staged deletion `sfincs-env.tar.gz` (G1 untrack); new `scripts/{analyze_d,setup_d_wind,setup_e_galibier,make_narrows_dredge,plot_narrows_dredge,rebuild_subgrid_h}.py` + `reports/figures/narrows_widen_h.png`. **NOTE: `data/` is GITIGNORED** → the `narrows_wide_h` catalog entry (in data/data_catalog.yml) + `data/elevation/narrows_wide_h.tif` + all `experiments/` live ON DISK ONLY (regenerable from the committed scripts). Suggested commit msg: "Add C/D/E/F/H experiment scaffolding + Galibier IG + narrows-widen diagnostic".

**APPROVED PLAN (2026-07-08, plan mode):** full plan saved at `/home/tpj8/.claude/plans/alright-claude-time-to-deep-alpaca.md`. Seven workstreams: A=validation apples-to-apples (re-derive Shrewsbury crest datum, partition HWMs by hydraulic basin not bbox, add tidal-range metric to validate.py); **B=subgrid-conveyance diagnostics (LEAD/FIRST — probe uv_havg/uv_nrep/uv_pwet at Shrewsbury narrows u-points vs real eHydro cross-section; literature says residual lives in subgrid conveyance INPUTS)**; C=NW wall/flux bracket; D=wind-magnitude sensitivity (+alt Holland vortex wind); E=decoupled-SnapWave IG re-test (GATED on F); F=SFINCS v2.4.0 Galibier upgrade (fixes SnapWave-IG + wavemaker-N bugs → our prior IG/wavemaker verdicts were on buggy code); G=cleanup (untrack sfincs-env.tar.gz, prune igwaves/igwaves_wind/wind_waves ~7GB, audit archive/models). **CONFIRMED SEQUENCING: B first → A/C/D on current build → upgrade F only when reaching E → E last → G anytime.** Nothing executed yet.

Related: [[project_bridge_dam]] (07-05 conclusion the deficit is distributed conveyance near structural ceiling), project_mask_boundary_cleanups (memory retired 2026-07-25) (already fought the MIRROR image on this edge — waterlevel boundary pinning back-estuary too HIGH → switched Navesink/mainland to outflow), project_noaa_boundary (memory retired 2026-07-25) (4-gauge NOAA switch), project_validation_roadmap (memory retired 2026-07-25).
