<!-- Raw Claude-memory file, archived verbatim 2026-08-05 when the memory store was
     collapsed. Its standing content was merged into the new memory set and/or
     CLAUDE.md; this copy exists so the merge cannot have lost a detail.
     Superseded — do not treat as current. -->


Bare `jupyter` is NOT on the default bash-tool PATH. Don't try `jupyter nbconvert ...` directly — it fails with "command not found".

- **⚡ USE THE LOCAL ENV: `/tmp/$USER/sfincs/bin/python` (kernel "Python (sfincs-local)"), NOT `micromamba/envs/sfincs/bin/python`.** Same env, unpacked to node-local `/tmp` by `hpc/vscode_node.sh` so imports hit local disk instead of GPFS — **pandas import ~16 s → ~1 s**, and the GPFS path makes every throwaway `python -c` painfully slow. Set up once per compute node by `hpc/vscode_node.sh` (needs `./hpc/pack-env.sh` re-run after any `micromamba install`). Falls back to the GPFS env only if `/tmp/$USER/sfincs` is missing (fresh node).
- **DON'T execute the viz notebooks yourself** (user pref, 2026-07-22): `nbconvert --execute` on the GPFS env takes forever, and the user runs them interactively in VSCode anyway. Edit the cells, state that they're ready, and let the user run them. See [[feedback_no_notebook_edits_during_run]] for the related "never touch the .ipynb mid-run" rule.

- **PATH UPDATE 2026-06-26:** `~/miniforge3` is GONE. The live env is the project **micromamba** env at `micromamba/envs/sfincs/bin/python`.
- **CORRECTION 2026-06-29:** in a fresh bash-tool shell the env is **NOT auto-activated** — bare `python`/`python3` resolve to `/usr/bin/python` (no scientific stack), and `~/miniforge3/...` 404s. Always spell out the interpreter from repo root: `./micromamba/envs/sfincs/bin/python`. AND for any pyproj/geopandas CRS op (`to_crs`, `estimate_utm_crs`, plotting) you must `export PROJ_DATA=$PWD/micromamba/envs/sfincs/share/proj` first, or it errors `proj_create_from_database: Open of .../share/proj failed`.
- **Notebook rebuilt fresh 2026-06-26** (see project_fresh_guide_rebuild (memory retired 2026-07-25)): the canonical `notebooks/sfincs-nj-sandy.ipynb` is now ~56 KB (was 49 MB), so `Read`/`NotebookEdit` work on it again. The nbstripout git **clean filter is active**, so the tracked copy stays small even after you run it (outputs stripped on `git add`). To generate/regenerate it wholesale, a Python `nbformat` builder script is the clean path.
- To EDIT notebook cells, prefer the native `NotebookEdit` tool (now viable again post-rebuild). Older caveat (the 49 MB file blew the 25k-token Read cap so Read/NotebookEdit both failed) no longer applies unless the working file re-accretes outputs without being committed/stripped.
- If you DO rewrite the `.ipynb` JSON by hand, match the existing serialization or git explodes into a 1000-line whitespace diff: `json.dumps(nb, indent=1, ensure_ascii=False)` + a trailing newline. The notebook stores Unicode RAW (em-dashes, →, ², −), so the default `ensure_ascii=True` re-escapes every such line. A plain `nbformat.write` round-trip is ALSO noisy here (not byte-clean). Verify with a no-op round-trip → empty `git diff` before trusting a hand edit.
- **NEVER `git checkout`/`git restore` the notebook to "reset formatting."** The quadtree notebook's day-to-day work (e.g. the 2026-06-02 Phase-3 validation fixes: tiled downscale `nrmax=1000`, `nearest_wet_face`/`face_coordinates` quadtree sampling, de-rotate `reproject_match`) frequently lives UNCOMMITTED in the working tree — a checkout silently destroys it. (Happened 2026-06-02; recovered only because the cells were verbatim in the session transcript + VSCode Local History had partial snapshots at `~/.vscode-server/data/User/History`.) **Action for the user: commit the notebook often** so the working tree isn't the only copy.

The canonical notebook is [notebooks/sfincs-nj-sandy.ipynb](../../../notebooks/sfincs-nj-sandy.ipynb) — as of 2026-06-26 a **freshly rebuilt, config-driven step-by-step guide** (76 cells), NOT the old accreted build. The legacy build, the old regular-grid baseline, and the experiment notebooks all moved to the top-level **`archive/`** (gitignored junk drawer: `archive/models/` for the old `model_quadtree*` dirs, `archive/notebooks/` for old notebooks incl. `sfincs-nj-sandy-legacy.ipynb`). Tim Leijnse's reference notebook stays tracked at `notebooks/reference/`. The new build writes to **`../model`** (was `../model_quadtree`). See project_fresh_guide_rebuild (memory retired 2026-07-25) and project_housekeeping_2026_06_25 (memory retired 2026-07-25). Related: [[project_nj_sandy]], project_validation_roadmap (memory retired 2026-07-25).
